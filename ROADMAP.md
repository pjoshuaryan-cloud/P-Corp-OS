# Roadmap

**Status:** Active — Phase 1 underway as of 2026-07-20.

## Purpose

Sequence the work so P Corp OS gets built "slowly, correctly, layer by layer" without stalling or getting rushed — per FOUNDER_BRIEF.md → Development Philosophy.

## Phases

**Phase 0 — Foundations. Complete as of 2026-07-20.** Documentation and repo structure (this repo), `TECH_STACK.md`'s full decision set, and `ENGINEERING_MANUAL.md`. No production code was written, per the brief's own instruction. Trading robot remained the active priority throughout.

**Phase 1 — The interview. In progress, started 2026-07-20.** Multi-session process where Frank (via Claude) interviews Joshua to build a mental model of his personality, communication, decision-making, values, goals, risk tolerance, etc. (full list in FOUNDER_BRIEF.md → Before Frank Exists). This happens before any Layer 2 code, not alongside it. Open thread within this phase: how interview content gets recorded, especially for sensitive categories (marriage, health, finances) — flagged in `MEMORY_SYSTEM.md`/`SECURITY.md` as needing privacy handling that doesn't exist yet, so nothing sensitive is being committed to this repo verbatim until that's resolved with Joshua directly.

**Phase 2 — Platform layer (Layer 1). Started 2026-07-23.** Build the minimum P Corp OS platform needed to support one real use case end to end.

Begun with the desktop UI shell — a static SwiftUI layout (sidebar, War Room center, right rail), per Joshua's own phased UI plan (visual shell first, interactivity and intelligence layered in afterward). By 2026-07-24 the shell had grown well past static: real navigation, dark mode, a working particle-based presence for Frank, keyboard shortcuts, an app icon, and the visual/logo work all landed — see `UI_GUIDELINES.md` for the full decision log.

**Backend started 2026-07-24.** First real Python service (`backend/`, FastAPI via `uv`) proving the IPC contract decided in `TECH_STACK.md` — a `/health` endpoint and a `/ws` WebSocket that streams responses back — and the SwiftUI shell's "Talk to Frank" input is now genuinely wired to it. Started echo-first to prove the IPC plumbing independent of LLM complexity, then upgraded the same day to real Claude reasoning: the plain `anthropic` SDK (not the Agent SDK — see `TECH_STACK.md` for why), a real system prompt distilled from `PERSONALITY_SPEC.md` (`backend/app/personality.py`), and conversation persistence in SQLite (`backend/app/db.py`) — one continuous conversation, loaded from disk on connect, matching "there is only ever one Frank." **Verified end-to-end 2026-07-24**, sooner than the "next week" originally planned: Joshua added a real Anthropic API key to `backend/.env`, and the full path — SwiftUI "Talk to Frank" input → WebSocket → Python backend → real Claude reasoning → streamed response back into the UI, with SQLite persistence confirmed across separate connections — genuinely works. First real conversation with Frank in the project's history.

**Typed memory records, same day.** `MEMORY_SYSTEM.md` — previously a stub — now has a working first layer: durable facts/context (`backend/app/memory.py`, `backend/app/db.py`), separate from raw conversation history, reusing the existing Claude Code memory scheme's four types. Frank's first tool call: one hardcoded `save_memory` tool, deliberately distinct from (and much smaller-risk than) the deferred Agent SDK. Verified: Frank saved a real memory mid-conversation, and a fresh connection recalled it unprompted.

**Memory made visible, same day.** One of the shell's 9 placeholder sections ("Frank") is now real — a read-only view of everything in `memory_records`, backed by a new `GET /memory` endpoint. Closes the loop: memory isn't just something Frank privately writes to disk anymore, Joshua can actually see what's been remembered.

The full per-device-keypair auth and `SMAppService` packaging haven't been started — flagged as explicit next steps, not forgotten. Real *agentic* Frank (general tool-use, the Agent SDK) is still Phase 3, gated on `SECURITY.md`'s permission model.

**Phase 3 — Frank (Layer 2) + first agent (Layer 3).** Stand up Frank against one real, low-risk agent rather than all twelve at once. Candidate: a read-only Trading Division assistant, since that domain is already deeply understood (see `TRADING_DIVISION.md`).

**Phase 4+ — Expand agents, deepen Alpha Mode and Trading Division integration, build out desktop War Room UX.** Order not yet decided — depends on what Phase 3 reveals.

## Explicitly not decided

- No calendar dates. The brief is explicit about not rushing; a roadmap with dates on a 10-year platform would be exactly the kind of speculative fiction the founder brief warns against.

## Priority check-in (2026-07-24)

Asked directly, after a status review against `FOUNDER_BRIEF.md`'s "Current Priority" section (which names the trading robot as the actual priority, P Corp OS as the long-term background project): several consecutive sessions of substantial P Corp OS build time had accumulated, worth a deliberate check rather than assuming momentum should continue by default. **Joshua confirmed: trading robot stays the priority.** P Corp OS work pauses here, not abandoned — Phase 2 is mid-flight (working shell, backend, and first memory layer), just not the next thing to pick up by default.

**Resumed 2026-07-24, explicitly at Joshua's request** (not by default/momentum — matches the guidance above). Asked which Layer 1 gap to build next (auth, cloud sync, or `SECURITY.md`'s permission model); Joshua chose `SECURITY.md`, since it's the explicit gate already blocking general tool-use and eventually the Trading Division integration. Now has a real, decided permission model (threat model scoped to what actually exists today, a three-tier tool classification borrowed directly from the same framework governing Claude's own actions this whole project) — see `SECURITY.md`. Deliberately document-only that pass: the local-auth-token gap it identified was kept as a separate, explicit next step.

**The local-auth-token fix, a real chat thread, and conversation sessions, 2026-07-25.** The backend now checks a machine-generated shared secret (`backend/app/auth.py`) on every route — closes the "any local process can connect" gap `SECURITY.md` flagged. War Room got its first real chat thread (previously only ever showed the single most recent reply as one line, despite the backend persisting full history since day one). Building it surfaced real usage feedback that reopened the earlier "one continuous conversation" decision: Joshua wanted to start fresh chats once he actually used the thread, then asked how to get back to an older one. Reconciled, not reversed — durable memory (`memory_records`), not the raw transcript, is what actually makes "there is only ever one Frank" true, so conversations can now start, list, and switch like any normal chat app (`conversations`/`app_state` tables, a new-chat button, a conversation switcher). Two real bugs surfaced by using it fixed same day: the "+ Mission" button visually compressing once two more icon buttons crowded the top bar, and empty test conversations burying the one real conversation in the switcher. Also fixed same day: the greeting going stale ("Good morning" all afternoon) since it only recomputed when something else re-rendered the view, not on its own as time passed.

**Real macOS notifications, 2026-07-25/26.** Closes Layer 1's "Notifications" responsibility (`FOUNDER_BRIEF.md`) — `TECH_STACK.md` had already decided the mechanism (WebSocket carries unprompted pushes from Frank), this just built it. `UNUserNotificationCenter` hard-crashes on this still-unbundled app; fell back to `osascript`, which doesn't need a real bundle identity. Triggered by `save_memory` firing — verified fully live: Frank correctly asked for confirmation before saving a name that didn't match what was on file (a real, unprompted "never assume" moment, not scripted), confirmed it, and a real notification appeared. Also surfaced a process lesson: running raw test scripts directly against the live backend (same database real conversations live in) can shift which conversation is "active" without meaning to — nothing was lost, but worth care going forward.

**Forgetting/versioning for memory records, 2026-07-26.** Closes `MEMORY_SYSTEM.md`'s flagged gap — `memory_records` only ever accumulated before this, no update or delete existed. Soft-delete (`deleted_at`), not a real DELETE, keeping it reversible and matching `SECURITY.md`'s "regular" tier for both memory tools. Two paths, one mechanism: Frank's new `forget_memory` tool (matches by title) and a manual trash icon in `FrankView`. "Versioning" is deliberately forget-then-resave, not a version-history schema. Verified end-to-end both ways, then confirmed live — Joshua used the actual trash icon to forget a real memory himself.

**Calendar section built, 2026-07-26** — the first of the 8 remaining placeholder nav sections to become real. Picked deliberately over the other 7: Trading Division is blocked by `TRADING_DIVISION.md`'s own sequencing, Alpha Mode Media/Finance/Personal all need external data sources or touch unresolved sensitivity questions, Agents/Automations are UI for Phase 3 capabilities that don't exist yet — Calendar needed none of that, just real local macOS data. `EventKit` silently failed (no permission dialog ever appeared, confirmed with a standalone test) — same root cause as the earlier `UNUserNotificationCenter` crash, this app still has no real bundle. Fell back to AppleScript (`SystemCalendar.swift`), same pattern as notifications. Real events now show in a new `CalendarView` and in the War Room right rail's "Today's Agenda" card (the old hardcoded placeholder data removed entirely).

**Knowledge section built, 2026-07-26** — second of the 8 placeholder sections. Deliberately not a generic file browser: a real doc viewer for this project's own foundational markdown files (`FOUNDER_BRIEF.md`, `ROADMAP.md`, `TECH_STACK.md`, etc.) — Frank's own knowledge starts with the documents that define him. Read directly off disk, no backend involvement. A small hand-rolled markdown renderer (`SimpleMarkdownView`), scoped to exactly what these docs use (headings, lists, inline bold/italic/links) rather than a third-party package.

**`SMAppService` packaging, stage 1 done — a real `.app` bundle, 2026-07-26.** None of the remaining 6 placeholder sections had as clean a "ready now" angle as Calendar/Knowledge did (real external dependencies, unresolved sensitivity questions, or Phase 3 prerequisites), so asked directly and picked this instead. `desktop/build_app.sh` now assembles and ad-hoc signs a real bundle — three real problems found and fixed along the way (not guessed around): codesign rejecting SPM's own resource-bundle placement, stray resource-fork extended attributes on source artwork, and — the more consequential one — a properly-launched bundle's working directory is `/`, not `desktop/`, silently breaking every cwd-relative path (`AuthToken.swift`, `KnowledgeDocs.swift`) the raw-executable era had assumed; fixed via `ProjectPaths.swift` deriving the repo root from `#filePath` instead, independent of runtime cwd. Also confirmed directly: a real signed bundle gets normal Dock/activation behavior automatically — the manual workaround for the raw executable is provably no longer needed for this launch path, though it stays in place since `swift run` remains the everyday dev workflow. Verified live by Joshua: Dock icon appeared immediately, shortcuts and all sections work through the real bundle. Stages 2 (embed Python + a native shim) and 3 (register via `SMAppService`) are separate, not started.

**`SMAppService` packaging, stage 2 done — the Python runtime is embedded, 2026-07-28.** `desktop/build_app.sh` now downloads a relocatable CPython 3.12.13 build (python-build-standalone), installs the exact locked backend dependencies into it, compiles a thin native C shim (`backend_shim.c`), and embeds both in the real bundle — decoupling from this dev machine's own `uv`-managed venv and system Python, the actual point of this stage. Deliberate scope call: the shim's `PYTHONPATH` still points at the real repo's `backend/` source rather than a bundled copy, since `db.py`'s SQLite path is relative to its own file location — bundling a copy would silently fork Frank's memory into a second database depending on launch method, breaking "there is only ever one Frank." Real bug found and fixed along the way: `load_dotenv()` searches from the process's working directory by default, not the file's own location — since the shim can launch from anywhere, this silently left the API key unset the first time a real conversation was attempted through it; fixed the same way stage 1's cwd bug was, deriving the path from `__file__` instead. Verified with escalating real tests: the runtime survives being moved to a different path, the real backend module imports and serves `/health` through it standalone, then a genuine live Claude conversation end-to-end through the actual signed bundle. Stage 3 (registering the shim via `SMAppService` as a persistent LaunchAgent) is separate, not started — the shim still needs manual launching for now.

**`SMAppService` packaging, stage 3 done — Frank is a real persistent background service, 2026-07-28. This closes out `TECH_STACK.md`'s full 3-stage packaging plan.** `BackendService.swift` wraps `SMAppService.agent(plistName:)`, wired to the existing (previously stubbed, no-op) "Launch at Login" toggle in Settings. Real bug found via the actual unified system log (not the shell-builtin `log` that was silently shadowing `/usr/bin/log` and swallowing predicates): the LaunchAgent registered and was approved cleanly, but never actually started — `launchd`'s `posix_spawn` was failing with ENOENT because the plist's `ProgramArguments[0]` was just the bare executable name, and `SMAppService`/BTM does not rewrite that to be bundle-relative before handing it to `launchd`, contrary to what the API's framing implied. A real runaway respawn loop (retrying every ~10s against the broken config) was caught and stopped via `launchctl bootout` before it caused any problem. Fixed with the executable's real absolute path. **Verified thoroughly, not just "it registered":** killed the backend process directly and confirmed `launchd`/`KeepAlive` restarted it automatically within seconds; quit the GUI app entirely and confirmed the backend kept serving real requests regardless — genuine independence, the actual point of this whole 3-stage effort, not just a process that happened to be running.

**Push-to-talk voice input, 2026-07-28.** Finally scoped and built the original ask (particle blob hidden while texting, present and voice-reactive while talking) — properly scoped as its own dedicated piece of work, not bolted onto something else, per the earlier deferral. Confirmed decision: push-to-talk, not continuous/always-listening — smaller privacy footprint, no wake-word/VAD complexity. `VoiceInput.swift` uses `Speech`/`AVAudioEngine` directly — privacy-gated frameworks in the same category as `EventKit`, which failed outright pre-packaging; worth trying specifically because `SMAppService` packaging now gives the app a real signed bundle. `FrankOrb` takes a real, optional live audio level (mic RMS) blended with its existing synthetic shimmer, and reappears specifically while listening regardless of an active text thread — directly closing the original visual ask. Real bug found through live testing: the first version's `stop()` cancelled the recognition task immediately, truncating real speech (confirmed: a full sentence collapsed to "And it's so") — fixed by waiting for the recognizer's own final callback instead of assuming the click itself delivers the result. Retested with a complete sentence afterward and confirmed clean. Text-to-speech (Frank's replies spoken aloud) is a separate, later stage — this is voice input only.

**Full searchable, date-grouped conversation history, 2026-07-28.** Direct response to Joshua wanting Frank to remember all chats indefinitely and be able to reopen any of them. Retention was already indefinite — no expiry logic anywhere in `db.py`, confirmed by inspection rather than assumed — so the actual gap was findability, not persistence: the existing conversation switcher was a small flat list with no way to search or see when something last happened. `list_conversations` (`backend/app/db.py`) now computes `last_message_at` per conversation (`MAX(messages.created_at)`) and sorts by it instead of creation order, and accepts an optional `q` param doing a real `LIKE` search across every message's content, not just the first-message preview — exposed via `GET /conversations?q=`. `ConversationListPopover` (`WarRoomView.swift`) rewritten in place (not a new UI) with a debounced search field and sections grouped by day ("Today", "Yesterday", "Sunday, Jul 26", ...). Real bug hit and fixed along the way: the persistent `SMAppService`-managed backend doesn't hot-reload Python source changes, so it kept serving stale responses (missing `last_message_at`, ignoring `q`) until `launchctl kickstart -k` forced a restart — worth remembering now that Frank runs as a real background service rather than something restarted by hand on every edit. Verified live: search for "Joshx" correctly isolated the one matching conversation among several, with correct day-grouped labels.

**Text-to-speech, auto-stop voice input, fresh conversation per launch, 2026-07-28.** Three direct pieces of feedback from actually using push-to-talk and the conversation history feature, all fixed same day. (1) Frank now speaks replies aloud via on-device `AVSpeechSynthesizer` (`VoiceOutput.swift`) — only for turns that started as voice input, never for typed ones — played through a real `AVAudioEngine` graph specifically so the PCM buffers could be RMS-analyzed and fed into `FrankOrb`'s shimmer, closing the audio-reactive-output requirement `UI_GUIDELINES.md` had flagged as unbuildable until real voice existed. Voice selection filters for `gender == .male` before ranking by quality — the first version ignored gender entirely and silently picked a female system default; confirmed live after Joshua downloaded a real Enhanced/Premium male voice via System Settings' Read & Speak pane (the actual fix for "too robotic" — no installed voice on this Mac was above default quality, a ceiling no selection logic alone could fix). (2) Push-to-talk no longer needs a second click to end a turn — `VoiceInput.swift` auto-stops once real speech has been detected and then gone quiet for ~1.2s, with a 60s safety cap and manual click-to-stop kept as an early-exit option. (3) A brand-new conversation now starts automatically once per app launch (`BackendClient.swift`) rather than resuming whatever was last active — old ones don't just sit there, they stay fully reachable through the search/date-grouped history browser. Implemented as a `static`, not instance, flag specifically because War Room's view (and its `BackendClient`) gets recreated on every sidebar navigation, not just on launch — an instance-level flag would have wiped an in-progress conversation just from clicking to Settings and back. All three verified live by Joshua directly.

**Cloud voice, real-world voice-quality fixes, and shorter replies, 2026-07-29/30.** Direct live-usage feedback chain, same overall thread as the text-to-speech work above. Joshua tried the on-device male voice and still found it "too robotic" — checked directly rather than assumed: this Mac's only downloadable en-US male voice ("Tom") tops out at Enhanced quality, no Premium tier exists for it at all, a real ceiling no amount of selection logic could fix. Tried ElevenLabs (genuinely human, stylized voices) — free tier turned out to block API access to Voice Library voices entirely; built a genuinely free, fully local fallback instead (Piper, `app/piper_tts.py`, MIT-licensed, no account, runs on the same embedded backend) when "forever free, no payment" was the actual ask — then Joshua decided the specific "dark and tough" character voice was worth paying for after all. `/speak` now tries ElevenLabs first when configured and falls back to Piper on any failure (missing permission, quota, payment lapse) — kept both paths rather than picking one, since two different real ElevenLabs failures already happened in one sitting. Real latency bug found and fixed: a fresh `httpx.AsyncClient` was being opened per request, paying a full DNS+TLS handshake every single call (confirmed directly: the identical request timed at 4.9s cold vs 1.1s warm) — one client now lives for the app's lifetime and is reused. Auto-stop-on-silence threshold (`VoiceInput.swift`) was a pure guess (0.015) with no real recording to check against — actual speech on Joshua's setup peaked at only ~0.005 RMS, so it silently never triggered at all; fixed to 0.002 from real logged data (captured via `os_log`/`Logger`, not `print()` — confirmed directly that this GUI app's stdout goes to `/dev/null` once fully activated, not just buffered, the same "unbundled executable" quirk class that's bitten this project before). Separately root-caused a hard crash: push-to-talk only works through the properly signed `.app` bundle (`build_app.sh`) — `swift run`'s raw executable has no Info.plist at all, so TCC hard-crashes the instant it requests speech-recognition access; this had been silently invalidating earlier test rounds. New: microphone capture is now pinned to the Mac's built-in mic specifically (`BuiltInMicSelector.swift`, Core Audio device selection by transport type) regardless of the system's current default input — Joshua's AirPods were forcing Bluetooth's low-bandwidth call-quality codec (HFP) for simultaneous input+output, badly hurting recognition accuracy, a real hardware/protocol limitation affecting every app, not just this one; output (TTS) is untouched and still plays through whatever's connected. Also added an explicit brevity constraint to `personality.py` — "concise" was stated but never enforced, and replies were unnecessarily long, worsened for voice since a long reply means a long wait before Frank even starts speaking. All fixes verified live: clean, consistent transcripts, short replies, and correct ElevenLabs audio confirmed by Joshua directly ("yes, all better now").

**Alpha Mode Media business data, and mobile access via Tailscale, 2026-07-31.** Two real pieces of scope, same session. First attempt at "Alpha Mode Media" (the placeholder section) was a real local brand-assets file/video browser — scrapped after a genuine `AVKit` `VideoPlayer` crash (a Swift-runtime metadata failure, confirmed via crash report, not a bug in this project's own code) and, more importantly, direct feedback that a generic asset browser wasn't the actual ask: Joshua wants a real business-management system (clients/projects/invoices/deliverables) that exists as its own thing, with Frank just able to report on it — not another P Corp OS tab. Built `alpha_mode_db.py` (a genuinely separate SQLite file, `alpha_mode.db`, not commingled with `pcorp.db`) and five new Frank tools (`add_client`, `add_project`, `add_invoice`, `add_deliverable`, `update_alpha_mode_status`) mirroring the `save_memory`/`forget_memory` pattern — no management UI yet (explicitly deferred as its own, much bigger, later build), so Frank is the only way data gets in for now. Current business state is summarized into his system prompt every turn, same mechanism as memory records. Verified end-to-end with a real conversation (Frank correctly created linked client/project/invoice records, then accurately reported the live snapshot back), then cleaned up the test data and test conversation content before committing.

Separately: mobile access, via a private Tailscale network rather than the public internet — deliberately the lightweight path (a single dependency-free HTML page, `app/mobile.html`) rather than a native iOS app, since building a whole second app against a Frank that's "far from complete" was the wrong order of operations (both device installs done by Joshua himself — account setup isn't something to do on his behalf). The backend now runs a second `uvicorn` listener bound specifically to this Mac's Tailscale-assigned IP — deliberately not `0.0.0.0`, which `SECURITY.md` already ruled out since it would also expose the backend to the local Wi-Fi network; the desktop app keeps using `127.0.0.1` with zero dependency on Tailscale being installed or even running. The token can be passed once via a `?token=` URL parameter (saved to the browser, not re-typed on a phone keyboard) rather than requiring manual copy/paste. Added auto-reconnect with backoff after a real disconnect report — confirmed directly via a health check that the backend itself was fine, so this is iOS Safari's own routine background-tab suspension, not a bug; the page now recovers on its own instead of needing a manual reload, including reconnecting immediately when the tab becomes visible again rather than waiting out the backoff timer.

**iOS "Add to Home Screen" for the mobile page, 2026-07-31.** Direct follow-up once mobile access worked: gives the page its own icon (the real P-mark `app_icon.png`, resized to 180×180 and served via a new `/static` mount) and launches full-screen with no Safari chrome, using the standard `apple-mobile-web-app-*` meta tags. Real, self-inflicted bug found through live testing: the token-in-URL bootstrap was stripping its own query string right after saving it (via `history.replaceState`, meant to keep the token from lingering visibly in the address bar) — but "Add to Home Screen" saves whatever URL is currently showing as the icon's launch link, so by the time the token had been silently removed, the saved icon never actually got it. Confirmed directly (a freshly added icon kept prompting for the token, same as no token had ever been provided) before concluding it was this, not an iOS storage quirk. Fixed by no longer stripping the token from the URL — visible-in-the-address-bar is the accepted trade-off for the icon reliably working every time.

**Agents — Frank's first delegated specialist (Operations Agent), plus general task tracking, 2026-07-31.** Scoped through direct conversation before building anything, same discipline as Alpha Mode Media: Frank stays the only agent Joshua talks to directly, and decides whether to answer himself or delegate. Operations is the first specialist — SOPs, workflows, project planning, task coordination, deadlines, checklists, bottleneck-spotting, automation suggestions. Architecturally the simplest version of delegation that's actually useful: a second Claude call with a distinct system prompt, invoked as a tool (`consult_operations_agent`) from Frank's own turn — not a nested agentic loop or a separate persistent conversation, which would be real complexity not needed yet. Only task tracking (title/status/area/due date) gets real persistent storage (`operations_db.py`, its own SQLite file, same "genuinely separate domain" reasoning as Alpha Mode Media's) — the rest of the responsibilities stay advisory-only for now, not saved anywhere unless Joshua asks Frank to remember something.

Two real bugs found through live testing, not guessed: (1) `max_tokens=1024` on both Frank's own turn and the Operations Agent's call was truncating actual multi-phase SOPs mid-document — confirmed directly, twice, in real use (Joshua's own testing surfaced this before it was ever reported to Claude directly, found while investigating the conversation history) — raised both to 4096. (2) The Operations Agent's call was non-streaming, so a genuinely long document sat completely silent for 60-70+ seconds before anything appeared; confirmed via a timed, isolated API call that this was normal generation speed for that length, not a slowdown, then switched to streaming its output directly to the websocket as it generates — dropped time-to-first-output from 67s to 7s in a retest, with the streamed content also appended to the persisted transcript so reopening the conversation later doesn't lose it. Also fixed in passing: chat bubbles were rendering literal `**bold**`/list markdown as plain text with visible asterisks — assistant messages now render through `SimpleMarkdownView` (already built for Knowledge), user's own messages stay plain.

**Agents nav section made real, chat typing indicator, and a working stop button, 2026-07-31.** Direct follow-up after the Operations Agent shipped: clicking "Agents" in the sidebar still fell through to the generic "Not built yet" placeholder, a real inconsistency flagged directly rather than left as-is — new `AgentsView.swift` shows Operations Agent as an active specialist plus its open tasks, read-only via a new `GET /operations/tasks` endpoint. Separately, direct feedback wanted a visible signal while Frank's replying, "similar to Claude": three pulsing dots now appear as the pending assistant bubble itself (in the chat, not the input bar — an earlier version put them in the input bar, corrected once actually seen), and the input bar's send arrow becomes a real stop button while streaming — not cosmetic, `BackendClient.stopGenerating()` actually closes the live connection (unwinding the backend's in-flight generation) and reopens a fresh one for the next message, deliberately without reloading history, since that would silently erase the partial reply still on screen.

**Voice-first input bar, and real proactive insights, 2026-08-01.** Direct feedback on the whole feel of the app: "P Corp OS still gives me more of a chatgpt vibe than something similar to jarvis." Traced to two concrete, separate causes. (1) Push-to-talk lived as a small icon in the top toolbar, physically separate from the actual input area — the literal reason voice read as an accessory rather than the primary way to talk to Frank. Moved into the input bar itself as the first, most prominent button, same visual weight as send; removed the decorative waveform icon that used to sit there doing nothing. (2) The right rail's "Frank's Insights" card was still literal hardcoded placeholder text — the clearest possible example of the app never volunteering anything real. Built `app/insights.py`, deliberately NOT LLM-generated (real hallucination risk and ongoing cost for what's actually just date comparisons against data that already exists — Alpha Mode Media invoices, Operations tasks): surfaces only overdue items or ones due within 7 days, with an honest "nothing overdue or due soon" state rather than always showing content regardless of whether anything's true. Real bug found on first live test: the card only fetched once at launch — it's part of the persistent right-rail layout, not a per-tab view recreated on navigation like `AgentsView` — so anything added afterward never appeared without restarting the app. Fixed with 30s polling in the view's own `.task`, since genuinely proactive shouldn't require a manual refresh button.

**Alpha Mode Media Admin (the real, separate Electron+Supabase app) found, fixed, and verified; Alpha Mode Agent's project/invoice writes now go live into it, 2026-08-02.** Full detail in `CHANGELOG.md`. Real macOS Gatekeeper malware rejection root-caused and fixed (Electron 31→43, not a security bypass); a genuine real-time-subscription bug in the app's own code found and fixed (auth-token sync + one-channel-per-table, not the environment/RLS as first suspected). Alpha Mode Agent's `add_project`/invoice-status tools now write directly into Supabase instead of a local copy that could drift.

**Six more delegated specialists, plus a dynamic Agents tab, 2026-08-03/04.** Full detail in `CHANGELOG.md`. Design, Creative Director, Communications, Memory, and Research Agents joined Operations and Alpha Mode Agent — every agent from `AGENTS_VISION.md` that isn't deliberately fenced off (Life, CFO, Trading, Engineering) is now built and verified live. The Agents sidebar section, previously one hardcoded card for Operations Agent only, now reads from a small backend registry (`app/agents_registry.py` + `GET /agents`) — confirmed genuinely dynamic across all five subsequent agent additions, each needing one registry entry and zero SwiftUI changes.

**Automations section built for real, event-triggered rules, 2026-08-04.** Full detail in `CHANGELOG.md`. Scoped through direct conversation first: event-driven ("whenever X happens, do Y"), not scheduled. `app/automations_registry.py` (one real rule so far: new Alpha Mode project → Design Agent suggests a folder structure) checked in the existing tool-dispatch loop, notification pushed via the existing `\n[notify]` mechanism, firings logged and surfaced in a new `AutomationsView.swift`. Verified fully live: a real Frank conversation created an actual Supabase project and correctly triggered the automation — then the test data was cleaned up before committing.

**Image upload in the War Room chat, 2026-08-06.** Full detail in `CHANGELOG.md`. With every unblocked agent and placeholder section built, asked directly what's next beyond the four still-fenced-off agents — grounded in `FOUNDER_BRIEF.md`'s Layer Two ("Frank reasons, communicates") and directly closing Design Agent's own scope-noted blocker (no image support existed for real critique). Real protocol change: the WebSocket's client→server message became a small JSON envelope so an optional image can ride alongside text, server→client streaming untouched. Verified with an actual screenshot sent through the real code path — Claude's response correctly and specifically described its contents, not a blind-trusted implementation.

## Next step

Of the original 5 remaining placeholder sections (Trading Division, Finance, Personal, Agents, Automations), **Agents and Automations are now both real** (above). Alpha Mode Media turned out to be a 6th, previously-uncounted placeholder — missed in this list originally since it wasn't part of that first count; **now also real** (2026-08-10, see `CHANGELOG.md`). Trading Division, Finance, and Personal keep their real blockers (separate-threads rule, finance caution, sensitive-material policy — same class as the fenced-off agents) and stay untouched until Joshua explicitly decides to revisit one. Per-device auth is now real (2026-08-10): the second device this gap was waiting on showed up — Joshua's iPhone 15, reachable over the Tailscale listener built 2026-07-31 but left dormant until re-enabled and verified live today (real chat with Frank from `/mobile`). Cloud sync (the other half of that original gap) is still genuinely open.

**Update (2026-08-12):** The native SwiftUI iOS app named above as "one live option" is now real, not just possible — Xcode installed, a new `PCorpKit` shared package extracted from `desktop/`, a first real screen built, and verified working on both the iOS Simulator and Joshua's actual physical iPhone 15 (real code signing with his personal Apple ID, real device trust, a real conversation created over the real Tailscale network — see `CHANGELOG.md`). Both follow-ups named at the time are now also done: the auth token moved to Keychain, and a real War Room screen (Mission Status/Insights/Situation Room stacked above the chat, all reusing `PCorpKit`'s existing clients) replaced the first pass's plain chat thread — verified live on Joshua's actual iPhone. Still just War Room, though — the other 10 desktop nav destinations (Agents, Automations, Alpha Mode Media, Settings, etc.) remain deliberately unbuilt on mobile, a real next round whenever Joshua wants it, not attempted in this one.

**Update (2026-08-14): the iOS app has full nav parity with desktop now — every one of the 11 sidebar sections is real, none left as placeholders.** Full detail across both days in `CHANGELOG.md`; summary here. Visual language was ported pixel-for-pixel from `AppTheme` (reversing an earlier iOS-native-colors call), and navigation moved from "just War Room" to a real hamburger-triggered slide-in sidebar — swipeable open/closed too, not just tap, after a real gesture-priority bug (the chat's `ScrollView` was winning touch arbitration against the open-swipe) was found and fixed. Voice input and image attach landed on the input bar, matching desktop's. A real mobile-specific bug was found and fixed the same week: the WebSocket never reconnected after the phone locked or the app backgrounded (`BackendClient.connect()`'s once-only guard is correct for desktop, which is never suspended, but silently wrong on iOS) — Frank would generate and store a real reply server-side that never reached the phone. Then, one at a time, the 7 sections still showing the honest "not built yet" placeholder all became real: **Frank** (memory records, `MemoryClient`) and **Settings** (including finally wiring the real manual dark-mode toggle, resolving the deviation flagged in the 2026-08-12 pass) and **Automations** and **Agents** were direct, unmodified ports of already-cross-platform `PCorpKit` clients; **Knowledge** needed a real new backend surface (`GET /knowledge`, `GET /knowledge/{filename}`) since a phone has no filesystem access to the Mac's repo the way desktop's local-disk read does, plus a push-based list-then-detail flow instead of desktop's fixed master-detail split; **Alpha Mode Media** was another direct port; **Calendar** is the one genuine architectural departure — reads the iPhone's own Calendar via `EventKit` rather than proxying the Mac's AppleScript-based read, a different (if usually equivalent) data source, flagged directly rather than glossed over. Also fixed along the way, all from live use: a disliked keyboard "Done" button (replaced with drag-to-dismiss), a flat "dark grey" `AppTheme.dark.surface` color (shared with desktop, so both platforms got richer card/input-bar contrast), and a delete-confirmation gap on Frank's forget button once hover (desktop's built-in "are you sure" buffer) had no touchscreen equivalent. AltStore/AltServer was explored as a free alternative to the $99/year Apple Developer Program (avoiding the current 7-day provisioning-profile manual-reinstall cycle) but abandoned after a real macOS-version incompatibility; staying on manual rebuild-and-reinstall with a scheduled reminder for now.

**Update (2026-08-10):** Working through the additive feature spec's audit table in "easiest first" order — real audit logging, Focus Lock, Decision Journal (capture-only), Memory Graph (linking-only), Shadow Mode (app-name-only activity log + on-demand recall), Opportunity Radar (one deterministic signal folded into the existing Insights card), Situation Room (escalated-severity alert banner, a deliberately separate surface from Insights), AI Debate (pressure-test a decision: case for, case against, honest verdict), and now Legacy Vault (succession/emergency info, save/list/delete, never credentials) are all built and verified live (see `CHANGELOG.md`). That's every item from the original audit table. The Agent Architecture framework-first question is now decided (2026-08-10): **keep the current per-module pattern** (each agent as its own file — a system prompt constant + a `consult_X_agent` tool schema + a dispatch function using its own isolated streamed Claude call), no shared base class or framework built. Joshua's own call, confirmed directly — nine specialists/tools built this way across the session with zero friction from the lack of shared infrastructure is real evidence the pattern scales fine as-is; a framework would be solving a problem that hasn't actually shown up yet. Revisit only if real duplication/friction actually surfaces adding a future agent, not preemptively. Decision Journal's UI/auto-context halves, Memory Graph's visual-UI half, and Shadow Mode's proactive-commentary half were all deliberately deferred too — revisit once there's enough real data to justify building the next layer on any of them. Opportunity Radar has real headroom too (more `leads` fields exist — quote status, contract status — than the one signal built so far), deliberately left for once the current signal proves useful in practice rather than building every plausible category speculatively. Situation Room's thresholds (30/14/45 days) are first-guess numbers, not tuned against real usage yet — revisit if they turn out too noisy or too quiet. AI Debate's broader reading (two personas debating each other over multiple turns) stays unbuilt too — a materially different, bigger feature, not started. Legacy Vault's actual-access-for-someone-else problem (export, printing, handoff to a lawyer) remains named but unsolved, deliberately.

**Flagged future direction (2026-08-10, not scoped or started):** real image generation — high-res photos, logos — most naturally extending Design Agent, which today can only critique/direct on images, never create one. Would need a real provider decision (Claude itself doesn't generate images; something like Google's Imagen or a comparable API) and a real per-generation cost, not a free addition. Video/animation generation named as a genuinely later step beyond that, not part of this round. Revisit when Joshua is ready to commit to a provider.
