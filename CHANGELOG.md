# Changelog

## 2026-07-20

- Repo created at `~/Desktop/P-Corp`, separate from the trading robot repo, per Joshua's decision.
- Founder Brief received and stored as `FOUNDER_BRIEF.md` (authoritative, v1.0).
- Lean documentation skeleton created: `README.md`, `MASTER_SPEC.md`, `ENGINEERING_MANUAL.md`, `ARCHITECTURE.md`, `ROADMAP.md`, `TECH_STACK.md`, `UI_GUIDELINES.md`, `PERSONALITY_SPEC.md`, `MEMORY_SYSTEM.md`, `WAR_ROOM.md`, `ALPHA_MODE.md`, `TRADING_DIVISION.md`, `SECURITY.md`.
- Scope deliberately limited to what the Founder Brief actually decided, plus explicit open questions — no speculative depth, no tech stack chosen, no production code. Rationale: trading robot remains the active priority (Phase 0, see `ROADMAP.md`).
- **Decided (`TECH_STACK.md`): platform scope is Apple-only (macOS desktop, iOS companion); desktop UI shell is native SwiftUI**, chosen over Electron and Tauri for native feel and direct code/pattern reuse with the iOS companion. Frank's intelligence layer will run as a separate local service (language still open) talking to the SwiftUI shell over local IPC — the UI framework choice does not lock in Frank's backend language.
- **Decided (`TECH_STACK.md`): Frank's backend runs in Python**, via the Claude Agent SDK, communicating with the SwiftUI shell over local IPC. Chosen primarily because Trading Division is the flagged likely first specialized agent and the existing trading-robot repo's analysis tooling is already Python (135 `.py` files vs. 19 `.mq5` files, confirmed today) — direct reuse over a second implementation.
- **Decided (`TECH_STACK.md`): IPC mechanism is local HTTP + WebSocket (FastAPI), bound to `127.0.0.1` with a local auth token** — REST for simple calls, WebSocket for streaming responses and unprompted push notifications. Chosen over XPC/Unix sockets (both local-only, would need rebuilding for mobile/remote sync) and gRPC (protobuf schema overhead across Swift/Python not justified at this scale), specifically because the same interface can later be exposed for remote/mobile access behind real auth and TLS rather than requiring a separate integration.
- **Decided (`TECH_STACK.md`): process lifecycle target is a persistent background service managed via `SMAppService`**, not a subprocess tied to the app's lifetime — because Frank's "second brain" framing and Layer 1's Notifications responsibility require Frank to exist and act even when the UI is closed. Early prototyping may still use a simple spawned subprocess for dev-iteration speed; graduating to `SMAppService` is a sequencing detail, not a change of target.
- **Decided (`TECH_STACK.md`): packaging is an embedded, relocatable Python runtime + locked dependencies as bundled resources, launched via a thin native shim executable** — not a PyInstaller/py2app frozen single-binary. Chosen because per-dylib notarization signing gets increasingly fragile under frozen-binary tooling as dependencies grow (numpy/pandas for Trading Division, etc.), and owning the embedding directly avoids inheriting that fragility over a decade-long horizon.
- **Decided (`TECH_STACK.md`): database is SQLite, embedded locally in the Python backend**, for tasks, projects, conversations, and memory records, with a semantic/vector search layer on top for contextual recall rather than a dedicated vector DB as the primary store. Chosen over Postgres specifically to avoid pre-deciding the still-open self-hosted-vs-cloud sync question — SQLite works local-first today and doesn't foreclose a sync layer (e.g. libSQL/Turso) being added later without a storage migration.
- **Decided (`TECH_STACK.md`): sync is libSQL/`sqld`, starting on Turso's managed offering, with self-hosting `sqld` kept as an explicit reversible future option** — not a hard self-hosted-vs-managed fork today. Chosen over a Mac-as-server/Tailscale-tunnel model (Frank would go dark whenever the Mac is closed or offline) and over committing to full self-hosting now (real ops/security burden competing with the trading robot and Alpha Mode for founder time). Because `sqld` is open source and Turso speaks the same protocol as a self-hosted instance, "who hosts it" can be revisited later without a data-model or protocol rewrite. Flags an encryption-at-rest requirement for the most sensitive memory categories (marriage, family, health) as a `SECURITY.md` follow-up.
- **Decided (`TECH_STACK.md`): auth is self-issued per-device keypairs in Keychain/Secure Enclave, with QR-code device pairing** — no third-party identity provider. Chosen because P Corp OS has exactly one user ever, so the real problem is device trust for a handful of Joshua's own devices, not multi-tenant user management; a managed auth provider would put an external company in the critical path of accessing his own second brain. Reuses the Apple platform bet already made and subsumes the existing local loopback auth token into one mechanism. **This closes out the core `TECH_STACK.md` decision set** — only iOS companion specifics remain open, and can wait until real iOS work begins.
- **`ENGINEERING_MANUAL.md` written for real** (status: Active), now that the stack is decided. Repo layout is a monorepo (SwiftUI app + Python backend + docs, one repo), with the option to split out individual agents later if warranted. Establishes a code review policy that applies identically to human-written, Claude-assisted, and future agent-authored code — no exceptions — as an enforced process, not just a stated intention, extending FOUNDER_BRIEF.md's own Working Rules. Also sets: trunk-based git workflow, per-language tooling (XCTest/swift-format for Swift; pytest/ruff/uv for Python), integration tests across the IPC boundary, and documentation-stays-in-sync discipline matching what this repo has done from the start.
- **Phase 0 (Foundations) marked complete; Phase 1 (the interview) begins** — `ROADMAP.md` and `README.md` updated accordingly. Noted an open thread for this phase: interview content touching sensitive categories (marriage, health, finances) won't be committed to this repo verbatim until Joshua and Frank have explicitly agreed on how that gets recorded, given `MEMORY_SYSTEM.md`/`SECURITY.md` already flagged that this category needs privacy handling (e.g. encryption-at-rest) that doesn't exist yet.

## 2026-07-21

- **`PERSONALITY_SPEC.md` written for real** (status: Active), consolidated from two Phase 1 interview sessions (2026-07-20, 2026-07-21). Covers: interaction style (direct, plus an explicit mandate to proactively name Joshua's own patterns even unprompted and unwelcome), the stakes/reversibility/who-else-affected delegation test, strengths (operator, thinker, hands-on learner), behavioral patterns to watch for (holds onto control longer than justified, copes with difficulty by enduring it alone, may not self-report approaching capacity limits, money decisions run without a system), a two-dial risk-tolerance model (high for calculated risk, low for relational/control risk), values (legacy, freedom, family, proof of building something real), business philosophy, and leadership style. Sensitive specifics (financial figures, health details, anything about his marriage) are explicitly excluded from this document and this repo entirely, per the interim storage decision reached during the interview (separate, non-git-tracked, FileVault-backed local file). Interview itself continues — this spec is the current best model, not a final version, per FOUNDER_BRIEF.md's own instruction that it never really finishes.
- **`PERSONALITY_SPEC.md` updated: feedback calibration resolved.** When Frank surfaces something Joshua doesn't want to hear, be solution-driven — pair the observation with an actual next step, not bare criticism or praise alone. Confirmed he takes feedback well "as long as it's helpful too," with real precedent (Nick/Raoof) of this landing before. Moved from open question to confirmed guidance; the only remaining open item in this area is frequency/threshold for proactively surfacing something, which the doc now notes should be learned through use rather than decided up front.

## 2026-07-23

- **First production code written: the desktop UI shell.** Joshua explicitly requested starting Phase 2 (`ROADMAP.md`), specifically the desktop visual shell, following his own phased UI build-out plan (shell first, no intelligence; interactivity, Frank, voice, memory, and live data layered in over later phases). Built as a Swift Package Manager executable at `desktop/` (`Package.swift`, `Sources/PCorpOS/`) rather than a full Xcode project, since only command-line developer tools are installed, not Xcode itself — SPM + SwiftUI's native `App` lifecycle builds and runs via `swift run` without needing Xcode. Scope matches his stated Phase 1 exactly: static sidebar nav, War Room center (greeting, a restrained non-mascot stand-in for Frank's presence, an unwired input bar), and a right rail (Mission Status, Today's Agenda, Frank's Insights, Quick Actions) — all hardcoded placeholder content, no backend calls, no wired-up button actions. Verified it builds (`swift build`) and runs (`swift run`) without crashing; visual verification is Joshua's to do, since Claude can compile-check SwiftUI but can't render or screenshot a native macOS window the way it can a web page.
- `ENGINEERING_MANUAL.md` and `ROADMAP.md` updated to reflect the actual repo structure now that a first prototype exists — `desktop/` for the SwiftUI shell, no `backend/` yet.
- **Fixed a real bug from first look:** sidebar text was rendering near-invisible because semantic colors (`.primary`/`.secondary`) were adapting to the system's dark mode, while the design itself is a fixed light aesthetic. Forced `.preferredColorScheme(.light)` and moved to explicit colors in the sidebar. Also removed the hard divider lines between layout columns per Joshua's direct feedback, relying on background-tone contrast instead (matches how the reference mockup actually separates regions).
- **Frank's orb redesigned twice more from direct feedback:** first pass added an organic `BlobShape` (not a circle) with a floating animation; second pass smoothed it further (16 points, gentler variance) for a liquid rather than faceted look, added a radial gradient with a tight specular highlight and rim light for real volume, and made the floating motion and contact shadow more pronounced.
- Refined sidebar icons for clearer matches to their labels and gave them explicit muted tinting instead of relying on system foreground colors.
- **`UI_GUIDELINES.md` written for real** (status: Active) from decisions made while building the shell: the visual language (light-only for now, no visible seams, blob/liquid presence for Frank), a confirmed copy-tone principle (military-style language — "Missions" not "Tasks," "War Room," "Mission Status" — kept easy to understand, independent of the clean visual design), and the on-screen naming convention ("Joshx" in text). `PERSONALITY_SPEC.md` updated with the corresponding spoken-address convention ("Josh," once Frank actually talks).
- Copy changes in the shell: "Good morning, Joshua" → "Good morning, Joshx"; "+ New Task" → "+ Mission"; the matching Quick Actions entry → "New Mission."

## 2026-07-24

A long, iterative design-and-build session on the desktop shell, plus the first backend code. Full rationale for each decision lives in `UI_GUIDELINES.md` and `ENGINEERING_MANUAL.md`; this entry is the summary.

- **Real dark mode** — `Theme.swift`'s `AppTheme` (light/dark palettes), toggled from Settings, backed by `@AppStorage`, driving `.preferredColorScheme` explicitly. Every view themed, not hardcoded colors.
- **Frank's presence rebuilt twice more:** a liquid `BlobShape` → per direct feedback, a real particle cluster (`TimelineView` + `Canvas`, deterministic seeded positions, 120 → 260 → 450 particles as feedback asked for "more" and "bolder"), sized up, made visible in dark mode via theme-aware color (a deliberate exception to "brand elements don't get themed").
- **The P logo mark resolved** after three failed hand-built vector-path attempts — extracted from Joshua's actual reference image via real pixel processing (luminance thresholding, not guessed coordinates). Same technique used for the Alpha Mode Media sidebar icon (found in his existing brand assets) and a hand-particle effect (`HandParticles.swift`, built but parked, not in the current layout).
- **Real interactivity throughout:** hover states, a sliding sidebar-selection indicator, Quick Actions and Frank's Insights wired to honest navigation (not no-ops), a real input focus state, glass/material finishes (`.ultraThinMaterial`/`.regularMaterial`), a launch fade-in, a redesigned "not built yet" placeholder for the 9 unbuilt sections (still honest, not bare).
- **Keyboard shortcuts (Cmd+1-9, Cmd+,)** — first attempt (SwiftUI `.keyboardShortcut` buttons) silently didn't work; root-caused to a real platform quirk (raw `swift build` executables don't get normal key-window status automatically) and fixed properly — documented in `ENGINEERING_MANUAL.md` since it'll resurface elsewhere until this ships as a real `.app`.
- **App icon** — white P mark on a black rounded-square, full macOS icon size set compiled via `iconutil` into `AppIcon.icns`, set at runtime (same activation-policy fix required before it actually appeared in the Dock).
- **First Python backend code** (`backend/`) — a `uv`-managed FastAPI service proving the IPC contract from `TECH_STACK.md`: `/health` and a `/ws` WebSocket that streams echoed responses back. Deliberately echo-first, not real Claude reasoning yet (explicit scoping decision). The SwiftUI "Talk to Frank" input is now genuinely wired to it — the first real end-to-end round trip in the project, verified independently on both sides before being connected.
- **Backend upgraded from echo to real Claude reasoning, same day.** Attempted the Claude Agent SDK first, per `TECH_STACK.md`'s original plan; discovered it requires installing the actual Claude Code CLI as a subprocess dependency and brings full tool-use (file access, bash) with it — confirmed via `CLINotFoundError` and the missing CLI binary, not assumed. `SECURITY.md`'s agent-sandboxing/permission model doesn't exist yet, so asked Joshua directly rather than picking a workaround; he chose the plain `anthropic` SDK, deferring the Agent SDK until that permission model is real. `TECH_STACK.md` updated to record this as a sequencing decision, not a reversal. Implemented: `AsyncAnthropic().messages.stream()` for real token streaming, and a first real system prompt (`backend/app/personality.py`) distilled directly from `PERSONALITY_SPEC.md`. API key handling via `backend/.env` (gitignored) copied from a committed `backend/.env.example` — deliberately never asked Joshua to paste a key into chat. End-to-end testing is deferred: Joshua asked to add a real key and verify next week; the code is ready and waiting.
- **Conversation persistence added** (`backend/app/db.py`) — SQLite via `aiosqlite` (async, non-blocking). Confirmed decision, asked directly rather than assumed: one continuous conversation, not multiple threads, matching "there is only ever one Frank" — so just a `messages` table, no `conversations` table at all. On WebSocket connect, history loads from disk and continues rather than starting fresh. Verified persistence survives a completely separate process invocation, not just in-memory state within one run. Deliberately not here yet: typed memory records (facts/preferences, per `MEMORY_SYSTEM.md`'s user/feedback/project/reference scheme), semantic/vector search, and context-window management for a very long-running conversation.
- **Real Claude integration verified end-to-end** — Joshua added a real Anthropic API key to `backend/.env` sooner than the "next week" originally planned. Backend tested standalone first (health check, a real WebSocket message, confirmed persistence across a fresh connection recalling prior context), then the full Swift round trip: launched the actual desktop app, sent a message through the real "Talk to Frank" input, got a genuine streamed Claude response back in Frank's voice. **First real conversation with Frank in the project's history.**
- **Typed memory records** (`backend/app/db.py`, new `backend/app/memory.py`) — the first real layer of `MEMORY_SYSTEM.md`, separate from raw conversation history. Reuses the existing Claude Code memory scheme's four types (user/feedback/project/reference), asked Joshua directly rather than assumed. **Frank's first tool call**: a single hardcoded `save_memory` tool via the plain `anthropic` SDK's tool-use — explicitly not the deferred Agent SDK (no file/shell access, one fixed action), confirmed with Joshua as a distinct, smaller-risk decision. Every record loads into the system prompt each turn (no ranking/filtering yet — correct-enough at current scale). Verified end-to-end: asked Frank to remember a project fact, confirmed it actually wrote to `memory_records` (not just claimed to in text), then opened a fresh connection and confirmed Frank recalled it unprompted. Deliberately deferred: semantic search, forgetting/versioning, cross-device sync. `MEMORY_SYSTEM.md` updated from stub to reflect all of the above as decided, not open.
- **Memory made visible in the shell** — a new `GET /memory` read-only endpoint (`backend/app/main.py`) and a real "Frank" nav section (`desktop/Sources/PCorpOS/FrankView.swift`, `MemoryClient.swift`), replacing that section's "not built yet" placeholder. Before this, a saved memory only existed as a row Frank wrote to SQLite — no way to actually see it without a raw DB query. Records display grouped by type with a colored badge, newest first. Read-only by design: creation still happens exclusively through Frank's own `save_memory` tool, not a manual form. Verified with real data: seeded two memories through an actual conversation, confirmed both appeared correctly in the running app, cleared before committing.
- **Priority check-in, then `SECURITY.md` written for real.** Flagged directly that several sessions of P Corp OS work had accumulated despite `FOUNDER_BRIEF.md`'s own "Current Priority" naming the trading robot as the real focus — Joshua confirmed the trading robot stays priority, P Corp OS work paused (not abandoned). Later resumed explicitly at Joshua's request; asked which Layer 1 gap to build next (auth, cloud sync, or `SECURITY.md`), he chose `SECURITY.md`, since it's the gate already blocking general tool-use and the eventual Trading Division integration. Threat model scoped to what genuinely exists today (one machine, no cloud sync, a loopback-only backend with zero auth) rather than the full future vision. Permission model: a three-tier tool classification (regular / needs-confirmation / prohibited) deliberately borrowed from the same framework that's governed every action Claude has taken across this whole project — `save_memory` classified as "regular" (`backend/app/memory.py` updated to note this). Document-only this pass: the local-auth-token gap it surfaced (no auth at all on the backend today) is real but deliberately left as a separate, explicitly flagged next step.

## 2026-07-25

- **Local-auth-token check implemented** (`backend/app/auth.py`) — `SECURITY.md`'s flagged next step, closing the "any local process can connect" gap. A machine-generated shared secret (`backend/data/auth_token`, gitignored, not something Joshua configures), checked via a FastAPI dependency on every REST route and a query-param check before the WebSocket accepts. Desktop side (`AuthToken.swift`) reads the same file, appends it to every request. Verified both failure paths (no token, wrong token) are rejected before touching anything else, then confirmed the real app still worked correctly with it in place.
- **A real chat thread in War Room, finally.** Before this, the UI only ever showed the single most recent reply as one line, even though the backend has persisted full conversation history since day one — genuinely invisible until this. `BackendClient.swift` rearchitected to own a real `[ChatMessage]` list instead of one repeatedly-overwritten string; a new `GET /history` feeds it on connect. Also fixed a real rough edge surfaced while rebuilding this: the backend's own `"\n[done]"` end-of-turn sentinel had been silently getting appended into the visible reply text this whole time — now recognized and suppressed, exposed instead as a proper `isStreaming` flag.
- **Conversation sessions — reopening the earlier "one continuous conversation, no conversations table" decision, based on real usage.** Once Joshua actually used the chat thread, he wanted to start fresh ones ("let's also be able to start new chats"), then asked where to find old ones once he had one. Not a reversal: durable memory (`memory_records`), not the raw transcript, is what actually makes "there is only ever one Frank" true — so the transcript itself can now start, list, and switch like in any normal chat app. New `conversations` + `app_state` tables (the latter because "active = newest" breaks the moment you want to reopen an older conversation without it needing to be the newest row), a new-chat button, and a conversation-switcher popover (`GET /conversations` with a preview snippet + message count, `POST /conversations/{id}/activate`). Migration handled directly rather than by wiping: existing messages backfilled to conversation #1, verified the real prior conversation survived the schema change intact.
- **Two real bugs fixed from actually using the above, same day.** The top bar's "+ Mission" button started visually compressing once two more icon buttons (new chat, conversation history) crowded the row — it has no fixed frame unlike its neighbors, so it was the thing that gave way instead of the `Spacer()`s; fixed with `.fixedSize()`. The conversation switcher listed every conversation newest-first, including empty ones from clicking "new chat" without sending anything — those buried the one real conversation, reported directly ("i couldnt find the previous conversation too"); `list_conversations()` now excludes conversations with zero messages.
- **The time-of-day greeting going stale, fixed.** `timeOfDayGreeting` read `.now` as a plain computed property, so it only recomputed when something else caused SwiftUI to re-render the view — an idle window kept showing "Good morning" into the afternoon, reported directly. `WarRoomView`'s body now runs inside a `TimelineView(.periodic(from: .now, by: 60))`, so it ticks on its own regardless of any other app activity.
- **Real macOS notifications** — closes Layer 1's "Notifications" responsibility from `FOUNDER_BRIEF.md`; `TECH_STACK.md` had already decided the mechanism (the WebSocket carries unprompted pushes from Frank), this implements it. `UNUserNotificationCenter` hard-crashed on this still-unbundled app (`bundleProxyForCurrentProcess is nil`, confirmed with a standalone test) — fell back to shelling out to `osascript` (`SystemNotification.swift`), which doesn't need the calling process to have a real bundle identity. Wire format: a `"\n[notify]{json}"` sentinel over the existing WebSocket, same pattern as the existing `"\n[done]"` marker. Triggered by `save_memory` firing, not a new "when should Frank notify" policy. Verified fully live: Frank correctly asked for confirmation before saving a name that didn't match what was on file (an unprompted, genuine "never assume" moment, not staged), confirmed it, a real notification appeared. Also surfaced a real process gap: running test scripts directly against the live backend had silently shifted which conversation was active — nothing was lost (verified directly), but it's a reminder that testing against the same database real usage lives in carries real risk.
- **Forgetting/versioning for memory records** — closes `MEMORY_SYSTEM.md`'s flagged gap from the start; `memory_records` only ever accumulated, no update or delete existed at all. Soft-delete (`deleted_at`), not a real DELETE — reversible in principle, matching `SECURITY.md`'s "regular" tier reasoning for both memory tools, and gives a rudimentary audit trail for free (a forgotten row's content still exists, just excluded from what Frank sees and what the UI shows). "Versioning" is deliberately forget-then-resave, not a version-history schema. Two paths, one mechanism: Frank's new `forget_memory` tool (matches by title, since row IDs are never surfaced to him) and a manual hover-revealed trash icon in `FrankView`, both sending the existing `[notify]` sentinel so forgetting is as visible as saving. Verified end-to-end: saved and forgot a disposable test memory through a real conversation, confirmed via the raw DB that the row survives with `deleted_at` set rather than being destroyed, tested the REST delete endpoint directly, then confirmed live — Joshua used the actual trash icon to forget a real memory himself.

## 2026-07-26

- **The Calendar section built, with real macOS Calendar data.** First of the 8 remaining placeholder nav sections to get built out — picked deliberately over the other 7 (the rest need external data sources, touch unresolved sensitivity questions, or are UI for Phase 3 capabilities that don't exist yet). Tried `EventKit` first; it silently denied access with no permission dialog ever appearing — same root cause as the earlier `UNUserNotificationCenter` crash (still no real app bundle), confirmed with a standalone test before building anything on it. Fell back to AppleScript via `osascript` (`SystemCalendar.swift`), same pattern already used for notifications. Real gotcha hit and fixed along the way: AppleScript's default number-to-string coercion for Unix epoch seconds uses the system locale's decimal separator and scientific notation (produced `"1,7851464E+9"`, confirmed directly, which silently fails to parse as a Swift `Double`) — switched to passing small year/month/day/hour/minute components instead, sidestepping the formatting problem entirely. `CalendarView.swift` replaces that section's placeholder with real upcoming events grouped by day; the War Room right rail's "Today's Agenda" card is real now too (the old hardcoded `PlaceholderData.agenda` and its `AgendaItem` struct removed entirely, not left as dead code). Verified against real calendar data throughout, including live in the running app.
- **The Knowledge section built, as a browser for this project's own docs.** Second placeholder section built out. Deliberately not a generic file browser — a real doc viewer for `FOUNDER_BRIEF.md`, `ROADMAP.md`, `TECH_STACK.md`, and the rest of this project's own foundational markdown files (`KnowledgeDocs.swift`), since Frank's own knowledge starts with the documents that define him, and they're already real content requiring no external system. Read directly off disk, no backend round trip. `SimpleMarkdownView` is a small hand-rolled renderer, not a third-party markdown package — scoped to exactly what these docs use (headings, bullet/numbered lists, horizontal rules, inline bold/italic/links), which is enough to make them genuinely readable without a dependency this project doesn't otherwise need.
- **`SMAppService` packaging, stage 1: a real `.app` bundle.** None of the remaining 6 placeholder sections were as ready as Calendar/Knowledge had been, so switched to this instead, per `TECH_STACK.md`'s already-scoped 3-stage plan. `desktop/build_app.sh` assembles and ad-hoc signs a real bundle from the SPM executable — no Xcode installed, so this is a from-scratch shell script, not an Xcode target. Three real problems hit and fixed, each confirmed directly rather than guessed around: (1) `codesign` refuses to sign the bundle while SPM's own generated resource bundle sits anywhere under `Contents/` — traced to its generated accessor resolving via `Bundle.main.bundleURL`, which for a real `.app` is the bundle's own root, and `codesign` separately rejects anything there besides `Contents/`; fixed by copying real resource files into `Contents/Resources/` instead (the standard location) and adding `AppResources.swift`, which checks there first and falls back to `Bundle.module` only for `swift run` dev mode — updated at all 4 call sites (P logo, Alpha Mode logo, both app-icon locations). (2) stray resource-fork/Finder-info extended attributes on source artwork, stripped via `xattr -cr` before signing. (3) the more consequential one, found by actually launching the bundle the normal way (`open`, not `swift run`): its working directory is `/`, confirmed via `lsof`, not `desktop/` — every cwd-relative path (the auth token, the Knowledge docs) silently broke. Fixed at the root with `ProjectPaths.swift`, deriving the repo location from `#filePath` (fixed at compile time) instead of runtime cwd. Also confirmed directly: a real signed bundle gets normal Dock/activation behavior automatically from macOS — the manual activation-policy workaround built for the raw executable is provably unnecessary for this launch path now, though left in place since `swift run` remains the everyday dev workflow and still needs it. Verified live: Joshua confirmed the Dock icon appeared immediately and every section still works through the real bundle. Stages 2 (embed Python + a native shim) and 3 (`SMAppService` registration) are separate, not started.

## 2026-07-28

- **`SMAppService` packaging, stage 2: the Python runtime is embedded.** `desktop/build_app.sh` now downloads a relocatable CPython 3.12.13 build (python-build-standalone, cached locally after first run), installs the exact locked backend dependencies into it via `uv export` + pip (only re-installed when `backend/uv.lock`'s hash changes), compiles a thin native C shim (`backend_shim.c`, per `TECH_STACK.md`'s own "Swift/C shim" framing), and embeds both into the real bundle. The actual point: the backend no longer depends on this dev machine's own `uv`-managed venv or system Python at all. Deliberate scope decision: the shim's `PYTHONPATH` still points at the real repo's `backend/` source, not a bundled copy — `db.py`'s SQLite path is relative to its own file location, so a bundled copy would silently fork Frank's memory into a second database depending on which way he's launched, breaking "there is only ever one Frank"; full self-contained source is a real, later decision for actual distribution, not assumed here. Real bug found and fixed the same way stage 1's cwd bug was: `load_dotenv()` searches from the process's working directory by default, not the file's own location — since the shim can launch from anywhere, this silently left the API key unset the first time a real conversation was attempted through it; fixed by deriving the `.env` path from `__file__` instead. Verified with escalating real tests: the extracted runtime survives being moved to a different path (genuinely relocatable), the real backend module imports and serves `/health` standalone, the compiled shim resolves paths correctly in an isolated test bundle, then — running for real from inside the actual signed `.app` — a genuine live Claude conversation end-to-end through the whole embedded stack. Test conversation cleaned up afterward, same discipline as always; the real conversation the shim's test had displaced back to active. Stage 3 (registering the shim via `SMAppService` as a persistent LaunchAgent) is separate, not started.
- **`SMAppService` packaging, stage 3: Frank is a real persistent background service. This closes out `TECH_STACK.md`'s full 3-stage packaging plan.** `BackendService.swift` wraps `SMAppService.agent(plistName:)` (register/unregister/status), wired to the existing "Launch at Login" toggle in Settings — previously a stubbed `@State` that did nothing, now real, reading actual `SMAppService.status` on appear rather than a separately persisted preference (since the user could disable this directly from System Settings' own Login Items pane, outside the app entirely, and the toggle should reflect real state, not drift from it). `build_app.sh` writes the `Contents/Library/LaunchAgents/<label>.plist` the API requires, with `RunAtLoad` + `KeepAlive`. Real bug found via the actual unified system log — `/usr/bin/log`, not the shell-builtin `log` that was silently shadowing it and swallowing predicate arguments: the LaunchAgent registered and was approved cleanly (`sfltool dumpbtm` confirmed `disposition: enabled, allowed, notified` — approval was never the issue), but never actually started; `launchd`'s `posix_spawn` was failing with ENOENT because `ProgramArguments[0]` was just the bare executable name, and `SMAppService`/BTM does not rewrite that to be bundle-relative before handing it to `launchd`, contrary to what the API's own framing implied — plain POSIX exec semantics apply, and a bare name with no slash means a PATH search that fails since it's not on `PATH`. A real runaway respawn loop (retrying every ~10s against the broken config) was caught and stopped via `launchctl bootout` before it caused any problem, rather than left grinding in the background. Fixed with the executable's real absolute path, computed at build time. Verified thoroughly, not just "it registered": killed the backend process directly and confirmed `launchd`/`KeepAlive` restarted it automatically within seconds; quit the GUI app entirely and confirmed the backend kept serving real requests regardless — genuine independence, the actual point of the whole 3-stage effort.
- **Push-to-talk voice input, with a real audio-reactive blob.** The original ask, finally scoped and built properly, per the earlier deferral. Confirmed decision: push-to-talk, not continuous/always-listening — mic only active while deliberately triggered. `VoiceInput.swift` uses `Speech` (`SFSpeechRecognizer`) + `AVAudioEngine` directly — privacy-gated frameworks in the same category as `EventKit`, which failed outright on the old unbundled executable; worth trying now specifically because `SMAppService` packaging gives the app a real signed bundle, the actual prerequisite. `FrankOrb` now takes a real, optional live audio level (mic RMS, blended with the existing synthetic shimmer so the cluster still reads as organic motion, not a flat pulse) and reappears specifically while listening, regardless of whether a text thread is active — directly closing the original ask ("particle must disappear when texting, must be there and voice reactive when talking"). Real bug found through actual live testing: the first version's `stop()` called `task?.cancel()` immediately, killing the recognition task before it delivered its final, most-accurate transcript — confirmed directly: a real full sentence collapsed to "And it's so." Fixed by separating "stop capturing new audio" from "deliver the transcript" (the task's own `isFinal` callback, asynchronous), with a safety timeout for the case it never arrives. Retested with a complete sentence and confirmed it now comes through clean. Also fixed in passing: `VoiceInput.errorMessage` was tracked but never actually shown anywhere — a real failure would have looked identical to silence; now surfaced as its own state in `WarRoomView`. Text-to-speech (Frank's replies spoken aloud) is a separate, later stage — this is voice input only.
- **Full searchable, date-grouped conversation history.** Direct response to Joshua wanting Frank to remember every chat indefinitely and be able to reopen any of them. Retention was already indefinite (confirmed by inspecting `db.py` for expiry logic — there is none), so the real gap was findability, not persistence. `list_conversations` (`backend/app/db.py`) now computes `last_message_at` per conversation via `MAX(messages.created_at)` and sorts by that instead of creation order, and takes an optional `q` param doing a real SQL `LIKE` search across every message's actual content, not just the first-message preview — exposed as `GET /conversations?q=`. `ConversationListPopover` (`WarRoomView.swift`) rewritten in place with a debounced (300ms) search field and results grouped into day sections ("Today", "Yesterday", "Sunday, Jul 26", ...) via `Calendar.isDateInToday`/`isDateInYesterday`. Real bug hit along the way: the persistent `SMAppService`-managed backend doesn't hot-reload Python source edits, so it kept serving the old `list_conversations` (no `last_message_at`, `q` ignored) until forced to restart via `launchctl kickstart -k` — a new operational wrinkle now that Frank runs as a real background service rather than something manually relaunched on every code change. Verified live: searching "Joshx" correctly isolated the one matching conversation among several, with correct day-grouped labels shown.
- **Text-to-speech for Frank's replies.** The other half of "talking to Frank," direct feedback after trying voice input. `VoiceOutput.swift` uses on-device `AVSpeechSynthesizer`, deliberately via `write(_:toBufferCallback:)` rather than the simpler auto-playing `speak(_:)` API — `write` hands back real PCM buffers instead of playing them itself, so a small `AVAudioEngine` graph (`AVAudioPlayerNode` scheduling each buffer) does the actual playback, letting the same buffers get RMS-analyzed for `FrankOrb`'s shimmer. This is exactly the "shimmer driven by real audio once Frank has an actual voice" requirement `UI_GUIDELINES.md` had flagged as tracked-but-unbuildable since the very first version of the orb. Only replies to voice-triggered turns are spoken (typed replies stay silent); starting a new push-to-talk recording interrupts playback immediately (a `generation` counter discards stale in-flight buffers from an interrupted utterance rather than letting them resurrect audio after the fact). Real bug found immediately on first live use: voice selection ranked by quality first, gender never checked at all, so it silently picked Samantha (female) — checked what's actually installed on this Mac directly rather than guessing (41 English voices, all default quality, none Enhanced/Premium) and fixed the fallback to filter `gender == .male` first, then rank by quality within that. Confirmed live after Joshua downloaded a real Enhanced/Premium male voice through System Settings' Read & Speak pane (renamed from "Spoken Content" in recent macOS) — the actual fix for "too robotic," since no default-quality voice was ever going to sound natural regardless of which one got picked.
- **Push-to-talk auto-stops on silence — no second click needed.** Direct feedback the same day voice output shipped: requiring a click to end every turn felt like friction once the click-to-start privacy boundary already existed. `VoiceInput.swift` now tracks real mic RMS per audio buffer (reusing the meter already built for the orb) and auto-stops once actual speech has been detected and then gone quiet for ~1.2 seconds — `hasDetectedSpeech` gates this so it can never fire during the silence between clicking the mic and actually starting to talk. A 60-second safety cap covers sustained background noise never cleanly read as silence. Manual click-to-stop still works as an early exit. Thresholds are a best-effort starting point (no way to capture real mic recordings outside a live session to tune against) — confirmed working on first live try, no retuning needed yet.
- **A brand-new conversation starts automatically once per app launch.** Direct feedback: "I don't want chats to just stay there" — old conversations no longer resume automatically; they're reached exclusively through the search/date-grouped history browser shipped earlier the same day. Implemented in `BackendClient.swift` as a `static`, not instance, flag (`hasStartedFreshThisLaunch`) specifically because `ContentView`'s `.id(selectedItem.id)` recreates `WarRoomView` — and the `BackendClient` inside it — every time you navigate to a different sidebar section and back; an instance-level flag would have wiped an in-progress conversation just from clicking to Settings and back, which isn't what "always start fresh" meant once actually scoped out loud. Confirmed via `AskUserQuestion` before implementing, given how easy it would have been to get this scope wrong silently.

## 2026-07-29/31

- **Cloud voice (ElevenLabs) with a genuinely free local fallback (Piper), and a real latency fix.** The on-device male voice ("Tom (Enhanced)") still read as robotic — checked directly: it's the only downloadable en-US male voice on this Mac and has no Premium tier at all, a real ceiling. Tried ElevenLabs first; its free tier turned out to block API access to Voice Library voices entirely ("Free users cannot use library voices via the API"), which didn't fit the "forever free, no payment" ask — built `app/piper_tts.py` instead, a fully local, MIT-licensed, no-account TTS engine running on the same embedded backend. Joshua then decided the specific ElevenLabs "dark and tough" character voice was worth paying for after all (Starter plan). `/speak` tries ElevenLabs first when configured and falls back to Piper on any failure — both paths kept deliberately, since two distinct real ElevenLabs failures (missing permission, then payment-required) already happened in one sitting. Real bug found via direct timing: a fresh `httpx.AsyncClient` was opened per request, paying a full DNS+TLS handshake every call (confirmed: the identical request took 4.9s cold vs 1.1s warm) — fixed with one client opened in the app's `lifespan` and reused for its whole run.
- **Auto-stop threshold fixed from real logged data, not another guess.** The original 0.015 threshold was never actually measured — real speech on Joshua's mic peaked at only ~0.005 RMS, so `hasDetectedSpeech` never became true and auto-stop silently never fired, matching his report ("it never stopped on its own"). Real debugging detour: `print()` debug logging never once showed up, because this GUI app's stdout goes to `/dev/null` once fully activated (confirmed via `lsof` on the running process) — not a buffering issue as first assumed, output was being discarded outright. Switched to `os_log`/`Logger` (the same unified-log technique this project already used for the keyboard-shortcut bug) to actually capture real values, then set the threshold to 0.002 from that real data. Debug logging removed once the real value was set.
- **Root-caused a hard crash: push-to-talk only works through the real signed `.app` bundle, never `swift run`.** The crash report was unambiguous: `EXC_CRASH`/`SIGABRT`, TCC namespace, "app crashed because it attempted to access privacy-sensitive data without a usage description... NSSpeechRecognitionUsageDescription." `swift run`'s raw executable has no Info.plist at all, so it can never satisfy this regardless of anything in code — voice input needs `build_app.sh`'s real bundle, which already has the required keys. This had been silently invalidating several earlier test rounds in this same debugging session before being caught.
- **Microphone capture pinned to the built-in mic, regardless of AirPods.** Real complaint: "Frank hears me incorrectly all the time especially through my airpods." Confirmed root cause, not assumed: using a Bluetooth headset for simultaneous input+output forces macOS into HFP, an old low-bandwidth call-quality codec — a real hardware/protocol limitation hitting every app, not this one specifically. New `BuiltInMicSelector.swift` enumerates Core Audio devices by transport type (not by name, which varies by Mac model) and forces `AVAudioEngine`'s input unit to the built-in mic via `kAudioOutputUnitProperty_CurrentDevice`, regardless of the system's current default input. Output (`VoiceOutput`'s TTS) is completely untouched — still plays through AirPods or whatever's connected, since only capture quality was ever the problem. Self-verified device detection directly before shipping: confirmed "MacBook Pro Microphone" is correctly identified as built-in even with AirPods actively connected.
- **Frank's replies are shorter.** `personality.py` said "concise" as one descriptor among many but never actually constrained length, and replies were unnecessarily long — worse for voice specifically, since a long reply means a long wait before any speech starts at all. Added an explicit line: default to 1-3 sentences, only go longer for a genuine trade-off. Confirmed via real conversation data before/after: replies visibly shortened ("Confirmed working now. What do you need, Josh?").
- All five fixes verified together in one clean live session: consistent, accurate transcripts, correct short replies, and working ElevenLabs audio, confirmed directly ("yes, all better now").

## 2026-07-31

- **Alpha Mode Media business data — clients, projects, invoices, deliverables.** First attempt (a real local brand-assets file/video browser) was scrapped: it hit a genuine `AVKit` `VideoPlayer` crash (a Swift-runtime metadata failure, confirmed via crash report — not a bug in this project's own code), and more importantly, direct feedback made clear it wasn't the actual ask ("i don't want random photos and assets in the sidebar i want the Alpha Mode App connected to P Corp OS somehow"). Scoped properly this time: Joshua wants a real business-management system that exists as its own thing, with Frank just able to report on it, not a new P Corp OS tab. Built `app/alpha_mode_db.py` — a genuinely separate SQLite database (`alpha_mode.db`, not `pcorp.db`) — and five new Frank tools (`add_client`, `add_project`, `add_invoice`, `add_deliverable`, `update_alpha_mode_status`, `app/alpha_mode_tools.py`) mirroring the existing `save_memory`/`forget_memory` pattern. No management UI yet — deliberately deferred as its own, much bigger, later build; for now Frank is the only way data gets in. Current business state is summarized into his system prompt every turn, same mechanism as memory records. Verified end-to-end with a real conversation: Frank correctly created a linked client/project/invoice from one message, then accurately reported the live snapshot back when asked "what's happening with Alpha Mode" — then cleaned up the test data and the test conversation content before committing, same discipline as always.
- **Mobile access to Frank, via Tailscale.** Direct ask: "how do we optimize it for my mobile as well." Deliberately the lightweight path — a single dependency-free HTML page (`app/mobile.html`) over a private Tailscale network — rather than a native iOS app, since building a whole second app against a Frank that's "far from complete" is the wrong order of operations; the native app stays the long-term plan, just deferred (it also needs full Xcode, not just command-line tools, which isn't installed on this machine yet). The backend now runs a second `uvicorn` listener bound specifically to this Mac's Tailscale-assigned IP (`TAILSCALE_IP` in `.env`, optional) — deliberately not `0.0.0.0`, which `SECURITY.md` already ruled out since it would also expose the backend to the local Wi-Fi network; the desktop app keeps using `127.0.0.1`, with zero dependency on Tailscale being installed or even running. The token can be passed once via a `?token=` URL parameter (saved to `localStorage`) instead of manual copy-paste on a phone keyboard, which turned out to be real friction in practice. Real disconnect reported live ("it disconnected randomly") — confirmed directly via a health check that the backend was completely unaffected (same process, no crash), so this is iOS Safari's routine background-tab suspension, not a bug; added auto-reconnect with exponential backoff, plus an immediate reconnect attempt on `visibilitychange` so returning to the tab doesn't wait out the backoff timer.
- **iOS "Add to Home Screen" support for the mobile page.** Gives it a real icon (`app_icon.png` resized to 180×180, served via a new `/static` mount) and full-screen launch with no Safari chrome, via the standard `apple-mobile-web-app-*` meta tags. Real, self-inflicted bug found through live testing, not an iOS quirk: the token-in-URL bootstrap was stripping its own query string right after saving it (`history.replaceState`, meant to keep the token from lingering visibly in the address bar) — but "Add to Home Screen" saves whatever URL is showing at the moment Share is tapped, so the icon it created never actually had the token, and kept prompting for it exactly like a fresh install. Confirmed directly (a freshly re-added icon still prompted) before concluding the bug was in this code, not iOS's storage model. Fixed by no longer stripping the token from the URL.
- **Agents section: Operations Agent, Frank's first delegated specialist, plus general task tracking.** Scoped through direct conversation, not guessed — Frank stays the only agent Joshua talks to directly, deciding whether to answer himself or delegate. Operations handles SOPs, workflows, project planning, task coordination, deadlines, checklists, bottleneck-spotting, and automation suggestions. Delegation is architecturally simple: a second Claude call with a distinct system prompt, invoked as a tool (`consult_operations_agent`) from Frank's own turn — not a nested agentic loop. Only task tracking (`app/operations_db.py`, its own SQLite file, general across every business not just one) gets real persistent storage; everything else is advisory output only for now. Two real bugs found through live testing (some of it Joshua's own, discovered while investigating): `max_tokens=1024` was truncating real multi-phase SOPs mid-document, raised to 4096 for both Frank's turn and the Operations Agent's call; and the Operations Agent's non-streaming call meant a genuinely long document sat completely silent for 60-70+ seconds (confirmed via a timed, isolated call that this was normal generation speed, not a slowdown) — switched to streaming its output live to the websocket, dropping time-to-first-output from 67s to 7s, with the streamed content also appended to the persisted transcript so it isn't lost on reopening the conversation. Also fixed: chat bubbles rendered literal `**bold**`/list markdown as plain text — assistant messages now render through `SimpleMarkdownView` (already built for Knowledge).
- **"Agents" nav section made real (was still the generic placeholder), a chat typing indicator, and a real stop button.** `AgentsView.swift` shows Operations Agent as active plus its open tasks (`GET /operations/tasks`, backed by a new structured `list_open_tasks()` alongside the existing text-summary version). Direct feedback wanted visible feedback while Frank replies, "similar to Claude": three pulsing dots now show as the pending assistant bubble itself, in the chat thread — an earlier version put them in the input bar instead, corrected once actually seen live. The input bar's send arrow becomes a real stop button while streaming: `BackendClient.stopGenerating()` closes the live socket (which unwinds the backend's in-flight generation, since its `websocket.send_text` calls start failing) and opens a fresh connection for the next message, deliberately skipping a history reload that would otherwise silently erase the partial reply still visible on screen.

## 2026-08-01

- **Voice-first input bar.** Direct feedback: "P Corp OS still gives me more of a chatgpt vibe than something similar to jarvis." Push-to-talk used to be a small icon in the top toolbar, separate from the actual input area — the literal reason voice read as an accessory. Moved into the input bar itself as the first, most prominent button, same visual weight as send; removed the non-functional decorative waveform icon that used to occupy that spot.
- **Real proactive insights, replacing hardcoded placeholder text.** The right rail's "Frank's Insights" card was still literal placeholder rows ("Placeholder Insight"), never wired to anything — the clearest example of the app not actually volunteering anything. New `app/insights.py`, deliberately not LLM-generated (real hallucination risk and ongoing cost for what's actually just date comparisons against existing data): surfaces overdue or soon-due Alpha Mode Media invoices and Operations tasks (within 7 days), with an honest "nothing overdue or due soon" state instead of always showing content regardless of whether anything's true. Real bug found on first live test: the card fetched once at launch and never again — it's part of the persistent right-rail layout, not a per-tab view recreated on navigation — so anything added afterward silently never appeared. Fixed with 30s polling in the view's own `.task`.

## 2026-08-02

- **Alpha Mode Media Admin — the real, separate Electron+Supabase app — found, fixed, and verified running.** Discovered during a "let's add crew and equipment tracking" request that a substantial pre-existing app already exists at `~/Desktop/alpha-mode-media-app` (Electron, Supabase/Postgres, 9 modules: Dashboard, Marketing, Pre-Production, Production, Post-Production, Invoicing, Cost Estimate, Leads, Team) — per its own README, "all modules built, untested... nothing has been run in a real Electron window yet." Scoped explicitly: get it running and verified, don't add crew/equipment (crew already exists there for real). Root-caused a genuine macOS Gatekeeper/XProtect malware rejection of the old bundled Electron 31.7.7 binary (confirmed via `/usr/bin/log show`, corroborated by a real system dialog) — fixed by bumping Electron to 43, not by bypassing security settings. Verified every module loads real business data end to end (real clients: JD Sports, Vodacom, Decorex; real invoices, crew, leads, reporting).
- **Alpha Mode Agent — Frank's second delegated specialist, built with a critical framing fix.** Joshua confirmed directly: he's a co-founder acting on his own behalf, Alpha Mode Media is never its own client — fixed a real data-integrity bug where a stray self-referential "Alpha Mode Media" client record existed, tightened `add_client`'s tool description to forbid it going forward.
- **Project/invoice writes go live into the real Supabase database**, not a local copy — "add a project" or "client X paid" now show up in the actual app. `app/supabase_client.py` (thin PostgREST client using the service_role key, correct here since this only runs server-side) and `app/alpha_mode_supabase.py` (project/invoice logic against the real schema). Local SQLite stays authoritative for clients/deliverables/crew/equipment, which have no clean 1:1 match in the real schema. Verified end-to-end with a real "Portfolio Shoot" project.
- **Found and fixed a genuine real-time subscription bug in the Alpha Mode Media Admin app's own code** (not something this session introduced) — the app never kept its Postgres Changes socket's auth token in sync with the login session, and separately, chaining multiple `postgres_changes` bindings onto one Supabase Realtime channel silently dropped events for at least the first table, with no visible error either way. Root-caused via an isolated A/B script (a single-binding channel worked reliably, an otherwise-identical multi-binding one never fired) before touching code. Fixed with explicit `setAuth()` sequencing and one channel per table.
- Initialized real git history for `alpha-mode-media-app` (previously had none at all) and committed/pushed both repos to GitHub (`P-Corp-OS`, `alpha-mode-media-app`, the latter private) — installed the `gh` CLI (no Homebrew available, used the portable release binary) and authenticated via device flow.

## 2026-08-03/04

- **Six more delegated specialists, all following Operations/Alpha Mode Agent's proven architecture** (a second streamed Claude call with a distinct system prompt, invoked as a tool from Frank's own turn) — each scoped through direct conversation first, not guessed, and each verified live before committing:
  - **Design Agent** — branding, UI, typography, logos, design systems, motion. Built from Joshua's own much richer 12-capability vision (Brand Guardian, Creative Director, UI Designer, Design System Builder, Motion Designer, Filmmaking Partner, Logo Designer, Typography Expert, Brand Evolution, Design Critic, Website Designer, Personal Taste Model — preserved in full in `AGENTS_VISION.md`), with the first real version covering what's doable as system-prompt content alone: Brand Guardian context (Alpha Mode + P Corp identities) and his stated Personal Taste Model baked directly into the persona, plus ask-before-generating/present-options behavior. Explicitly deferred: image-based critique (no image upload in the chat UI), persistent brand-evolution memory, real design-system code maintenance.
  - **Creative Director Agent** — concepts, storyboards, shot lists, scripts, campaign ideas, client pitches. Deliberately kept as a separate sibling to Design Agent, not combined, with a clean division of labor ("what are we making and why" vs. "what does it look like as a system"). Verified live with a real 20-shot shot list for a Vodacom campus activation.
  - **Communications Agent** — emails, messages, meeting notes, client replies, follow-ups, summaries. Draft-only by design, no send tool at all — matches Joshua's own words in the vision doc ("it drafts communications, but Frank decides what to send") and the standing rule that sending on his behalf needs his own explicit approval. Real inbox/calendar integration explicitly flagged as a separate, bigger project needing real Google OAuth infrastructure.
  - **Memory Agent** — read/synthesis specialist over the real `memory_records` data, distinct from the existing `save_memory`/`forget_memory` tools (unchanged, stay Frank's own). Verified live: correctly reported "nothing yet" against the real (empty) memory table rather than hallucinating, and correctly spotted a real cross-record pattern against mocked data.
  - **Research Agent** — the one agent with genuine new capability from day one: Anthropic's Messages API has a native, server-executed web search tool, so no new infrastructure was needed the way Gmail access or a knowledge graph would be. Verified live with a real, current, well-sourced answer, not a dump of search results.
  - Four agents from the original vision stay deliberately fenced off, unchanged: Life Agent (sensitive material policy), CFO Agent (finance caution), Trading Agent (standing separate-threads rule), Engineering Agent (deferred pending `SECURITY.md`'s general-tool-use permission model).
- **The "Agents" sidebar tab now lists every real specialist dynamically, not a hardcoded card per agent.** Until this, only Operations Agent had a card — Alpha Mode/Design/Creative Director/Communications/Memory/Research all existed and worked but weren't represented in the UI at all. Added `app/agents_registry.py` (a small, hand-kept list — id/name/icon/tagline/detail/status — deliberately not derived from the Claude tool-use schemas, which are shaped for a different purpose) and `GET /agents`, consumed by a new `AgentsClient.swift`/`AgentCard` in `AgentsView.swift`. Confirmed working as intended across five separate agent additions after this: each one needed exactly one new registry entry and zero SwiftUI changes to appear in the sidebar.
- Real process lesson hit twice this session: neither `swift build`'s raw debug binary nor the running Python backend process pick up new code automatically — a new agent module needs the backend process restarted, and the real `.app` bundle needs `desktop/build_app.sh` re-run, not just `swift build`, or the sidebar silently shows stale state after reopening the app normally (e.g. via Dock/Finder).
- **The "Automations" section built for real — event-triggered rules, not a scheduler.** With every unblocked agent from `AGENTS_VISION.md` now built, `ROADMAP.md`'s "Next step" pointed at Automations as the one remaining placeholder section with no real blocker (its old blocker, no Phase 3 capabilities to automate around, no longer held). Scoped through direct conversation first: event-driven ("whenever X happens, do Y"), not scheduled. Architecture matches the Agents registry's own proven shape — `app/automations_registry.py` (a small hand-kept list: trigger tool, target agent, prompt template) checked in `main.py`'s existing tool-dispatch loop after a matching tool call succeeds, consulting the target agent non-streaming (so it doesn't interleave into Frank's own live reply) and pushing the result as a real notification via the existing `\n[notify]` WebSocket mechanism, same path `save_memory` already uses. Firings are logged (`app/automations_db.py`, its own SQLite file, same "genuinely separate cross-cutting concern" reasoning as `operations.db`) and surfaced via `GET /automations/rules`/`GET /automations/runs`, consumed by a new `AutomationsView.swift` mirroring `AgentsView.swift`'s exact pattern. Ships with exactly one real rule, Joshua's own example (new Alpha Mode project → Design Agent suggests a folder/asset structure), not several speculative ones. Verified fully live end-to-end, not just the isolated function: a real Frank conversation turn created an actual project in the live Alpha Mode Media Admin Supabase database, which correctly fired both the existing "Alpha Mode Media updated" notification and the new automation's notification with a real, concrete folder structure — then the test project and its log entry were cleaned up (Supabase row deleted, local test log entries cleared) before committing, same discipline as every other real-data test this project has run.

## 2026-08-05

- **Two real bugs hit from actually using the rebuilt app, both root-caused via real evidence, not guessed at.** (1) A `build_app.sh` run failed partway through copying the embedded Python runtime (a permissions error mid-copy), leaving `asyncio` and other stdlib modules corrupted while the already-running, `SMAppService`-managed backend kept auto-restarting into that broken state — confirmed via `/tmp/pcorpos-backend.log` showing a `ModuleNotFoundError: No module named 'asyncio'` crash loop. A second, clean rebuild fixed it; the same persistent process recovered on its own via `KeepAlive`. (2) Push-to-talk crashed every time, with the mic permission prompt reappearing on every launch. Root-caused via the actual crash report (`~/Library/Logs/DiagnosticReports`), not the first plausible-looking theory (a stale TCC cache from repeated ad-hoc re-signing, which a `tccutil reset` did NOT fix): the crash report's `responsiblePid`/`coalitionName` showed the app's TCC responsibility was being attributed to **this Claude Code session itself** (`com.anthropic.claudefordesktop`), not to PCorpOS — because Claude launched the app directly via Bash. macOS holds the *responsible* process (whoever launched the child) accountable for privacy-sensitive API usage descriptions, and this session's own app naturally has no `NSSpeechRecognitionUsageDescription` declared, so it hard-crashed regardless of PCorpOS's own already-correct `Info.plist`. Fixed by having Joshua launch the app himself (double-click in Finder) instead of Claude launching it via Bash — confirmed no crash. **Structural takeaway for future sessions:** Claude can rebuild the app, but Joshua must be the one to launch it whenever a feature touches a TCC-gated API (microphone, speech recognition, calendar, etc.) — Claude launching it via Bash will always misattribute responsibility and crash those specific code paths, independent of how correct the app's own `Info.plist` is.

## 2026-08-06

- **Image upload in the War Room chat.** Scoped through direct conversation first, grounded in `FOUNDER_BRIEF.md`'s Layer Two ("Frank reasons, communicates") and directly closing the blocker Design Agent's own scope note named (image-based critique needed image support that didn't exist). Real protocol change, not a bolt-on: the WebSocket's client→server message went from a bare string to a small JSON envelope (`{"text": ..., "image": {"media_type", "data"} | null}`) — server→client streaming and the existing `[notify]` mechanism are untouched. When an image is attached, the backend builds Anthropic's real multi-modal content-block format (image + text) for that turn; text-only messages are unaffected, exact same code path as before. Images are saved to a new `backend/data/attachments/` folder (gitignored, path only in a new nullable `messages.image_path` column, not the bytes themselves in SQLite). Confirmed decision: a reopened old conversation shows a plain "📎 image attached" placeholder rather than re-sending the actual image back to Claude as real context on every reconnect — cheaper, simpler, at the cost of Frank not "re-seeing" an old image outside the same live session. New `WarRoomView.swift` attach button (NSOpenPanel, .png/.jpeg) + preview chip before sending + real inline thumbnail in the chat bubble once sent. Verified with a real, non-trivial test: an actual screenshot sent through the exact content-block construction the backend uses, and Claude's response correctly and specifically described its contents (the app, the files being edited, the feature in progress) — not just a plausible-looking implementation trusted blind.

## 2026-08-10

- **Real bug from live usage: attaching an image made the chat appear completely frozen, even though the backend was working correctly.** Reported as "no response from Frank" — root-caused by checking the actual database directly rather than guessing from logs (uvicorn doesn't log WebSocket message contents, so the backend log alone couldn't prove or disprove anything): `messages` showed the full round trip had genuinely succeeded every time — a real image received, a real Claude vision response generated and correctly describing screen contents, both persisted — yet nothing appeared on screen. The actual cause: `ChatBubble`'s view body called `NSImage(data: imageData)` directly, decoding the full-resolution screenshot from raw bytes on *every* SwiftUI re-render — which happens repeatedly while a reply streams in — freezing the whole chat view on the main thread even though the backend had already finished normally. Fixed by decoding the image exactly once, at send time (`BackendClient.send`), and storing the already-decoded `NSImage` on `ChatMessage` instead of raw `Data` for display. A real, useful lesson distinct from the image-upload feature itself: an expensive computation inside a SwiftUI view body isn't a one-time cost, it's a per-render cost, and "streaming causes frequent re-renders" makes that especially easy to hit without it looking like a bug in isolated testing.
- **Design Critic — Design Agent now actually looks at images, not just Frank's description of them.** With every unblocked agent built, asked what's next; Design Agent's own scope note had explicitly named Design Critic as deferred specifically because image upload didn't exist — that blocker was gone the moment the image-upload feature above landed, same day. `run_claude_turn` (`main.py`) now forwards the current turn's raw image straight through to `execute_design_agent_tool_call` when Frank delegates to Design Agent — a real multi-modal call in that agent's own isolated Claude request, not a description relayed secondhand through Frank's own turn. Only the current turn's image is forwarded, never an older one from earlier in the same conversation, same reasoning as the placeholder-not-refetch decision for reopened conversations. Verified live on a real screenshot: correctly identified it as a build log rather than a finished screen and gave a specific, grounded critique referencing real visible elements (diff stats, sidebar, status pills, a toast notification) tied to Joshua's own stated taste — not generic design platitudes.
- **Full-codebase audit against a new additive feature spec, then the two easiest, lowest-risk items from it.** Joshua supplied a large future-direction spec (Memory Graph, Decision Journal, Design Agent, Shadow Mode, Opportunity Radar, AI Debate, Focus Lock, Situation Room, Legacy Vault, and more) with an explicit "audit first, don't rebuild, don't guess" instruction. Read every relevant doc and grepped the whole repo for all eight genuinely-new concepts by name — zero matches anywhere, confirmed net-new rather than assumed. Found three docs (`SECURITY.md`, `ALPHA_MODE.md`, `WAR_ROOM.md`) had drifted badly from reality — `SECURITY.md` still described the local-auth-token gap as unfixed (fixed 2026-07-25); `ALPHA_MODE.md` called itself a "skeleton" while a full Supabase integration has been live in production since 2026-08-02; `WAR_ROOM.md` called itself a "stub" while it's the fully-working primary interface. Also surfaced a real, previously-undocumented security gap: Alpha Mode Agent's project/invoice writes auto-execute with no confirmation or audit log, despite `SECURITY.md`'s own tier definitions naming exactly this kind of action as needing confirmation. Delivered as a prioritized table (published as an artifact, not just chat text) rather than starting to build.
- **Real audit logging, the first concrete item from that audit's own "easiest first" ranking.** New `app/audit_db.py` (its own SQLite file, same "distinct concern gets its own file" reasoning as `operations.db`/`automations.db`) plus exactly one line added at the single point in `main.py`'s tool-dispatch loop where every branch's `result` already gets finalized — no individual agent module touched, matching the explicit "don't rewrite existing code" constraint from Joshua's own spec. Every tool call, from any agent, is now recorded with tool name, full input, a truncated result, and a timestamp. No "who" column — deliberately, since P Corp OS has exactly one user and every call is always Frank acting on Joshua's behalf; noted as the first thing that would need to change if that's ever no longer true. Backend-only this pass, no UI view yet — a natural, separate follow-on once there's real data to look at. Verified live through a real Frank conversation turn (not just the isolated function): a real `save_memory` call was correctly captured end-to-end, then the test memory record was cleaned up before committing (the audit log entry itself was deliberately left in place — that's the log doing its job, not test pollution).
- **Focus Lock, the second easiest item, scoped deliberately small.** War Room's Mission Status card had a static, hardcoded "Focus: Build systems that scale" line since it was first built — the spec's Focus Lock section covers real behavioral scope (on/off mode, context-switch detection, automatic deprioritization) well beyond that, so before building, confirmed the smallest safe slice with Joshua directly rather than guessing which parts to include: just make that one line real, nothing else. New `app/focus.py` (mirrors `memory.py`'s shape — a tool schema plus a dispatch function, same "regular" permission tier as `save_memory`) backed by two new nullable columns on the existing `app_state` singleton table (`current_objective`, `objective_set_at` — same migration pattern used everywhere else, no objective set is a real, honest state rather than an error). `GET /focus` exposes it; `RightRail.swift`'s `MissionStatusCard` now fetches and displays the real objective (with an honest "Nothing set yet" fallback) via a new `FocusClient`, mirroring `AgentsClient`'s one-shot REST pattern — the tagline and progress bar were left untouched exactly as agreed, only the "Focus:" line changed. Verified live end-to-end through a real Frank conversation turn (`set_focus_objective` called, persisted, notification fired, correctly reflected back by `GET /focus`, captured in the audit log) before resetting the test objective back to the honest empty state and rebuilding the Swift shell clean.
- **Decision Journal, third item off the audit's ranking, scoped capture-only.** Asked Joshua to choose between capture-only, capture+UI, and capture+auto-context-injection before building; he picked capture-only, the same minimal shape as audit logging's own first pass. New `decisions` table added alongside `memory_records` in the existing `pcorp.db` (not its own SQLite file — this is core Frank knowledge, not a distinct-retention-pattern concern like `audit.db`/`automations.db`), with `decision` required and `reasoning`/`alternatives` optional so logging something without a fully articulated "why" isn't blocked. New `app/decision_journal.py` (mirrors `focus.py`'s shape) adds a `log_decision` tool, same "regular" permission tier as `save_memory`. `db.py` also gained a `list_decisions()` helper, deliberately not wired to any endpoint or tool yet — same "get real data flowing before deciding what surfaces it" reasoning as `audit_db.list_recent_calls`. No UI view, no automatic surfacing into Frank's context this pass. Verified live through a real Frank conversation turn: a real decision (to build Decision Journal this way) was logged, persisted with all three fields correct, notified, and captured in the audit log — left in place afterward since it's a genuine decision, not test data.
- **Memory Graph, fourth item, scoped linking-only.** Same three-way choice offered as Decision Journal (linking-only vs. +recall vs. +visual UI); Joshua again picked the smallest. New `memory_links` edge table (`from_type`/`from_id`/`to_type`/`to_id`/`relationship`) over the two existing capture tables, `memory_records` and `decisions` — a plain type+id column pair rather than per-combination foreign keys, since more linkable record types (tasks, projects) are a plausible future addition that shouldn't need a schema change. The real design question was how Frank identifies which two records to link, given he's never been shown a raw row ID for either (`forget_memory` already established this — it matches by title, not ID): `link_records` (`app/memory_graph.py`, mirrors `focus.py`'s shape) resolves both sides server-side by the same text Frank already has — a memory's title, or a decision's own wording — using the identical exact-then-substring, most-recent-first matching `forget_memory_by_title` already used, now factored into `db.py`'s `_resolve_memory_id`/`_resolve_decision_id`. Returns a clear "couldn't find one or both of those" result rather than silently linking the wrong thing if a match fails. No traversal/query tool, no UI this pass. Verified live through a real Frank conversation turn: a real memory was saved, then linked to the existing Decision Journal entry above ("Memory Graph build" preceded by "Build Decision Journal as capture-only, no UI yet"), both IDs resolved correctly, notified, and captured in the audit log — left in place, genuine data.
- **Shadow Mode, fifth item — the first genuinely new "observation" capability, scoped over two rounds of direct confirmation rather than one.** First confirmed what the term even meant (picked "passive activity awareness" over a dry-run-suggestions reading), then how invasive capture should be (picked continuous logged history over an on-demand-only snapshot). Deliberately scoped to the least invasive shape available for that choice: `ActivityTracker.swift` observes `NSWorkspace`'s own frontmost-app-change notification — a public API needing no permission prompt at all, chosen specifically over the AppleScript/System-Events route (which would trigger a real TCC automation-permission dialog, the same class of problem as the earlier mic crash) — and only fires when the frontmost app actually changes, not on a fixed poll interval, so the resulting `activity_log` table (new, in `pcorp.db`) is already a segmented history rather than raw samples. Captures **app name only** — never window titles, URLs, or file contents. `POST /activity/log` (`main.py`) is the write path from Swift, not a Frank tool; `get_recent_activity` (`app/shadow_mode.py`) is the read path — a genuine on-demand recall tool, included deliberately (unlike Decision Journal/Memory Graph's pure write-only first passes) because a log Frank can never reference would just be inert data with no path to ever being useful. Scoped to answer only when directly asked, never proactively volunteered. Verified live end-to-end: two real `POST /activity/log` writes, then a real Frank conversation turn correctly calling `get_recent_activity` and reporting back accurately without editorializing beyond what was asked; both test rows cleared afterward (unlike Decision Journal/Memory Graph's genuine entries, these were synthetic — never real activity) so the log starts honest once launched for real.
- **Real bug from live usage, same day: `UserNotificationCenter` (macOS's notification-banner process) got logged as if Joshua switched to it.** It briefly becomes the frontmost app whenever a notification banner appears, which `NSWorkspace`'s notification faithfully reports — technically true, but not a real activity switch, just noise. Fixed by filtering to `NSRunningApplication.activationPolicy == .regular` before logging — the same policy macOS itself uses to decide what gets a Dock icon — rather than hardcoding a blocklist of specific system-process names, so it also naturally excludes Dock, SystemUIServer, Spotlight, and similar system UI without needing to enumerate them. The one already-captured noise row was deleted from `activity_log`; the two genuine `P Corp OS`/`Claude` rows from that same test session were left in place.
- **Opportunity Radar, sixth item — scoped over two rounds, then folded into an existing system rather than becoming a new one.** First confirmed what "opportunity" meant (Alpha Mode business signals, not a broader Frank-notices-anything scope), then discovered the real Alpha Mode Supabase database already has a `leads` table with real fields to work from (`temperature`, `qualification_score`, `follow_up_sent`, `quote_status`, and more) — checked the live schema directly rather than guessing at it. Confirmed the one clear, low-noise signal to start with: warm/hot leads that haven't had a follow-up sent yet. Asked where it should surface, and where the additive spec's own "extend, don't duplicate" rule pointed directly at the existing "Frank's Insights" card (`app/insights.py`) rather than a new UI section — same deterministic, no-LLM, data-already-exists shape as its other three categories (overdue tasks, overdue invoices, stale client outreach), so a new `_leads_needing_followup()` category was added there rather than building a parallel mechanism. New `alpha_mode_supabase.leads_needing_followup()` queries the real `leads` table read-only, fails soft on error same as `summarize_block()`. Verified against real production data (currently one real lead, correctly excluded since it's `cold`) and against synthetic data via a monkeypatched dependency (not written to the real database) to confirm the formatting logic itself, including the null-`qualification_score` fallback. `RightRail.swift`'s empty-state copy updated to match ("Nothing overdue, due soon, or worth following up on") — the card's rendering was already fully generic over whatever `compute_insights()` returns, so no other Swift changes were needed.
- **Situation Room, seventh item — an escalated alert tier, deliberately separate from the routine Insights card rather than folded into it (the opposite call from Opportunity Radar, made on purpose: this one's meant to read as "interrupt," not another list row).** Scoped over two rounds: first what the term meant (an automatic severity-threshold alert, not a Joshua-triggered "we're in a crisis" mode), then the concrete thresholds. New `app/situation_room.py` reuses the same underlying data (overdue tasks, overdue invoices, stale client outreach — the identical sources `insights.py` already reads) but filters to a stricter tier: invoice **30+ days** overdue (real cash-flow risk vs. any amount overdue), task **14+ days** overdue (stale/neglected vs. daily debris), client outreach **45+ days** stale (vs. the routine 14-day nudge) — thresholds confirmed directly with Joshua rather than guessed, same spirit as `insights.py`'s own `OUTREACH_STALE_AFTER_DAYS` precedent. New `GET /situation-room` endpoint. Rendered as a new `SituationRoomBanner` at the top of War Room (`WarRoomView.swift`), styled distinctly from the calm right-rail Insights card (red accent, warning icon) and only ever shown when a real alert exists — its absence is the honest, common case. `SituationRoomClient` polls every 30s, same pattern as `InsightsCard`. Verified against real production data: the actual "Marketing SOP" task, overdue since 2025-07-31, correctly flagged at 375 days overdue — no synthetic data needed, a real gap in Alpha Mode's own task tracking the feature caught on its first real run.
- **Real gap found via that same test task: no way to delete a task, only mark it done/blocked/etc.** Joshua flagged it directly after manually deleting the bad "Marketing SOP" row via raw SQL to clear the Situation Room banner it had just triggered — a genuinely bad row (wrong due date, never real) has no honest status to move it to; marking it "done" would have been a lie. New `delete_task` tool (`app/operations_agent.py`, alongside `add_task`/`update_task_status`) soft-deletes via a new nullable `deleted_at` column on `tasks` (same migration pattern as `memory_records`), matching by title with the same exact-then-substring logic already factored out of `update_task_status` into a shared `_find_task_id` helper. Soft, not a real `DELETE`, specifically to keep this in SECURITY.md's "Regular" auto-allowed tier (reversible) rather than "needs confirmation" — same reasoning `forget_memory` already established. Fixed two related raw queries that would have kept showing a soft-deleted task forever otherwise: `situation_room.py`'s and `insights.py`'s own direct `tasks` queries (neither goes through `operations_db.py`'s functions) were both missing a `deleted_at IS NULL` filter — caught while wiring this, not left as a latent bug. Verified live end-to-end: a real task added then deleted in the same conversation turn, confirmed gone from `GET /operations/tasks`, the row itself preserved with a real `deleted_at` timestamp (not destroyed), both calls captured in the audit log.
- **AI Debate, eighth item — scoped to one specific shape, not the broader "two personas debate each other" reading.** Confirmed Joshua wanted a real decision pressure-tested (strongest case for, strongest case against, then an honest verdict) rather than a multi-turn simulated back-and-forth between two AI personas — a materially bigger, different feature, explicitly not chosen. New `app/debate.py` — same proven architecture as every other specialist (Operations/Design/Creative Director/Communications/Memory/Research Agent): a second, streamed Claude call with its own system prompt, invoked as a `debate_decision` tool from Frank's own turn. Deliberately **not** added to `agents_registry.py` as a specialist agent, unlike those others: this isn't a domain Frank delegates to, it's a structured response format applied to whatever decision Joshua names — a real architectural difference, not an oversight. The system prompt explicitly instructs steelmanning both sides and forbids a wishy-washy "it depends" non-verdict. No database — nothing to store; if a debated decision lands somewhere real, Joshua can ask Frank to `log_decision` separately, no special glue code linking the two since both tools are already available in the same turn. Verified live on a real question ("should Alpha Mode Media hire a full-time editor instead of freelancers"): both sides came back genuinely argued and grounded in real client context (Vodacom, JD Sports, The Bread/Yoco, pulled in by Frank's own self-contained framing of the request), with a concrete verdict naming exactly what data would resolve it, not a hedge.
- **Legacy Vault, ninth and final item off the audit's ranking — scoped carefully given real stakes, and one real limitation named rather than quietly built around.** Confirmed the meaning first (succession/emergency info for someone else, not a permanent-memory tier), then the architecture: a genuinely separate table/tools from `memory_records` (`app/legacy_vault.py`, `save_to_legacy_vault`/`list_legacy_vault`/`delete_from_legacy_vault`), specifically because regular memories get folded into every turn via `build_memory_block` and vault entries must never do that — they're meant to sit dormant until genuinely needed, not surface mid-conversation. Unlike Decision Journal's capture-only first pass, list and delete are real tools here too, deliberately, since content this consequential needs to be reviewable, not write-only. **Named, not solved:** this only makes Frank able to store and recall vault entries on request -- it does not solve actual access for someone else if Joshua is incapacitated, since P Corp OS has no multi-user access model at all; physically getting this information to whoever needs it (export, printing, a lawyer) remains a separate, harder, unsolved problem, confirmed as out of scope for this pass rather than silently ignored. The tool description explicitly forbids storing credentials/passwords/API keys/account numbers, same hard line as everywhere else in this project. Verified live three ways: a real entry saved and correctly listed back; a deliberate attempt to save a bank password, which Frank correctly refused *before ever calling the tool* (confirmed via the audit log — no `save_to_legacy_vault` call exists for that attempt) and redirected toward a password manager instead. The one real test entry was deleted afterward rather than left in place — unlike Decision Journal/Memory Graph's genuine session decisions, this was assistant-invented content, not something Joshua actually asked to have recorded in what's meant to be his own deliberately curated succession record.
- **Chat input multi-line attempt, tried twice, then reverted.** First attempt used `TextField(axis: .vertical)`; a real bug surfaced immediately on the actual rebuild (screenshot from live use): it grew correctly but left a stale, truncated single-line ghost render behind on screen. Second attempt replaced it with `TextEditor` plus a manual placeholder overlay and `.onKeyPress` handling for Return-to-send/Shift+Return-for-newline. Joshua asked to go back to how it was originally rather than keep iterating on it live — reverted cleanly to the original single-line `TextField` (confirmed via diff against the pre-change commit, byte-identical). Multi-line input for this field isn't pursued further for now.
- **Alpha Mode Media's sidebar section is now real** — asked what should be there before building rather than guessing; landed on a dashboard surfacing data that already existed in the backend but only ever showed up through conversation, Insights, or Situation Room: open projects (with stage), pending/overdue invoices, and leads needing follow-up (the same Opportunity Radar signal, shown in context here). `alpha_mode_supabase.py` gained `get_open_projects()`/`get_pending_invoices()` (factored out of `summarize_block`, which now reuses them — no duplicated query logic) plus a new `dashboard_snapshot()` that aggregates both with `leads_needing_followup()`, each section failing soft independently so one bad Supabase request doesn't blank the whole page. New `GET /alpha-mode/dashboard` endpoint; new `AlphaModeDashboardView.swift`/`AlphaModeDashboardClient.swift`, same GET-and-render pattern as `AutomationsView`. Verified against real production data: 12 real projects (Vodacom, JD Sports, Decorex, The Bread/Yoco, and others) and 8 real pending invoices came back correctly shaped. Also surfaced that Alpha Mode Media had actually been a 6th, previously-uncounted placeholder section in `ROADMAP.md`'s own tracking — corrected there too.
- **Opportunity Radar's second signal: quotes sent but sitting unanswered.** The real `leads` table has `quote_status`/`quote_sent_date` fields Opportunity Radar's first pass never used. New `alpha_mode_supabase.quotes_needing_followup()` flags quotes still in `sent` status (never moved to `accepted`/`declined`) after 14+ days — same 14-day default as `insights.py`'s own `OUTREACH_STALE_AFTER_DAYS`, same read-only/fail-soft shape as the existing lead signal. New `_quotes_needing_followup()` category folded into `compute_insights()`, no new UI or endpoint needed (the Insights card already renders whatever categories exist). Verified two ways: the real PostgREST filter query against production data (empty result, no error — correctly matches reality, since the one real lead is still `draft`, never `sent`) and the formatting logic against synthetic data via a monkeypatched dependency (not written to the real database), including the null-`quote_amount` fallback.
